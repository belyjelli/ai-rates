# Plan: Perp Funding & Price-Spread Index Platform (ORBIT-style)

## Context
We're building our own version of orbitperpscreener.com. It's a free public site, paid for through exchange referral links, that combines perpetual-futures data from about 57 venues (CEX, DEX and Hyperliquid HIP-3 dexes) into four tools:

1. **Funding spread screener** (`/screener`)
   - For each asset, the widest funding APR gap: long on the lowest-rate venue, short on the highest.
   - Filters: min OI, min volume, asset class (crypto/RWA), venues, interval.
   - Sorting: predicted APR, 24h/7d settled average, stability, OI.
2. **Backtester** (`/pair/{SYM}?long=&short=&size=10000&days=30`)
   - Replays the real settled funding for both legs and subtracts fees and slippage.
   - Outputs: net PnL, $/day, payback, win rate, equity curve.
3. **Price-arbitrage screener** (`/arbitrage`, `/price-pair/{SYM}`)
   - Executable bid/ask gaps for the same asset across venues, after fees and with a size limit based on book depth.
4. **Exchange/asset index** (`/markets`, `/markets/exchange/{venue}`, `/markets/asset/{SYM}`)
   - Per venue: OI, volume, market count, fees, points/TGE status, daily momentum.
   - Per asset: a funding table across all venues plus the best pair.
   - Plus `/trade`, a referral hub.

Later: blog/glossary, airdrop calendar, en/ru, Telegram alerts.

**Constraints you chose:** Bun + TypeScript, Python workers, **100% Cloudflare hosting and storage** (no external DB), all ~57 venues, free + referral monetization, a 2–3 developer team. The repo `ai-rates` is empty (greenfield).

---

## Architecture

```
        watchdog cron (*/5)  ──ensureAlarm()──►  VenueCollectorDO  ×57   (one per venue; locationHint in the DO name, e.g. "binance@apac-ne")
                                                   │ alarm every 60s: pool ≤5 fetches, token bucket, 40s deadline, circuit breaker
                                                   │ all exchange calls happen HERE (queue/cron handlers ignore placement)
                        ┌──────────── RPC ─────────┼──────────────── RPC ─────────────┐
                        ▼                          ▼                                  ▼
               VenueHistoryDO ×57           MarketHubDO (singleton)            Pipelines binding (1 batch per venue/cycle)
               SQLite ≤10GB each:           latest cross-venue matrix,          → R2 Iceberg tables (Parquet):
               settled funding_events       spreads, price arb, venue health    ticker_1m, funding_snap_1m,
               (+mark_px), 5-min snapshots  → JSON served via Cache API         orderbook_samples, funding_events
               (30d), 1h rollups (1y)                │                                   │
                        ▲                            │                                   ▼
                        │ RPC (2 legs)               │                          R2 SQL (open beta) /
                ┌───────┴────────────────────────────┴──┐                       DuckDB in py container
                │  web worker: Hono /v1/* API + React    │                                  │
                │  Router v7 SSR pages, Cache API edge   │◄── D1 "meta" ◄──── py-analytics Python Worker (hourly cron):
                │  cache, rate-limit binding, Turnstile  │    (venues, assets,  stability, 7d/30d avgs, momentum,
                └────────────────────────────────────────┘     referrals, derived  nightly "verified" backtests (numpy)
                                                               stats, bt cache)
   py-backfill Container (on demand): full CPython + ccxt → historical funding/klines backfill → VenueHistoryDO + R2
   (fallback) relay Container, constraints.regions=["APAC"]: egress proxy only for venues that fail the geo-probe
```

### Key decisions
| Concern | Decision | Why |
|---|---|---|
| Deployables | **One JS Worker `airates` at the repo root** (`wrangler.jsonc`, `main: apps/worker/src/index.ts`). It merges the planned `ingest` (DOs + watchdog cron) and `web` (API + SSR) services and deploys through Cloudflare Workers Builds (root `/`, deploy command `npx wrangler deploy`, runs on every push to `main`). Python lives in separate Workers Builds projects: `py/analytics` (Python Worker) and `py/backfill` (Container). A Worker is either JS or Python, so Python can't share the root Worker. The optional `relay` container is declared in the root `wrangler.jsonc` if needed. | Matches the existing deploy pipeline. One Worker keeps DO RPC local with no service bindings. |
| Polling fan-out | One DO per venue using self-rescheduling alarms. Each alarm has its own 6-connection limit and 15-min wall clock. Tiers: bulk funding+tickers 60s; per-symbol-only endpoints (e.g. OI) rotate through symbols; orderbooks every 10 min for pairable markets only. | Fits Worker limits (6 outbound connections, CPU) and venue rate limits. |
| Geo-blocking (Binance 451 for US IPs, Bybit CloudFront 403 on cloud IP ranges, dYdX indexer GEOBLOCKED; one CF community report of scheduled Workers getting 403s from Binance/Bybit/Kraken) | **Fallback ladder per venue, chosen by the Phase 0 probe:** (1) direct call from a DO with a location hint in its name (`binance@apac-ne`); (2) the same call through a Container pinned to `regions:["APAC"]` or `["WEUR"]`; (3) **CF-only relayed rates**: Hyperliquid `predictedFundings` and Lighter `/api/v1/funding-rates` both republish Binance/Bybit funding (rates only, no OI/book, marked "relayed" in the UI); (4) a tiny non-CF egress proxy (Tokyo/Frankfurt VPS), the only exception to CF-only, used only if you approve it after the probe. | Hints and placement pick a data center, not IP reputation. Blocks are IP-based. |
| Hot data | MarketHubDO holds the latest matrix. The screener JSON uses Cache API `s-maxage=15, stale-while-revalidate=60`. Clients poll every 15s (no WebSockets in MVP). | KV's 60s minimum TTL is too slow. Funding moves slowly. |
| Settled history (exact, used by backtests) | **VenueHistoryDO SQLite**, one per venue: `funding_events(asset, settled_at, rate, interval_h, mark_px)`, `snap_5m` (30-day retention via alarm), `rollup_1h` (1y). About 350 markets × 24/day ≈ 3M rows/year per venue, well under 10 GB. | Shards naturally by venue. A backtest is just two RPC reads. Exact data, no sampling. |
| Relational metadata | **D1 `meta`**: venues (referral URL, perks, fees, points/TGE), assets + symbol map, markets, derived stats, backtest cache. | Small and relational. Fits D1 well. |
| Raw firehose and research | **Pipelines → R2 Iceberg/Parquet** (≈20k markets × ~200 B/min ≈ 70 KB/s, far below the 5 MB/s limit). Queried with R2 SQL (GROUP BY, window functions, joins supported) or DuckDB in the Python container. | Cheap, complete archive for replay/reprocessing and momentum/stability analytics. |
| Backtester (interactive) | **TypeScript in `packages/core`**, running in the `web` worker. Two legs × 30 days ≈ 1.5k points. | No Pyodide cold start. The same math is unit-tested and shared. |
| Python's role | `py-analytics` Python Worker (Pyodide, numpy/pandas, hourly cron with a 15-min CPU budget) computes stability scores, settled averages, momentum and nightly verified top-10 backtests, then writes to D1. `py-backfill` Container (ccxt, pyarrow/duckdb) runs historical backfills. | Matches the Bun/TS + Python requirement and puts Python where it's strongest. |
| Frontend | React Router v7 (framework mode) on Workers + Tailwind + TanStack Table (virtualized) + lightweight-charts. SSR renders the first table rows. The Hono `/v1` API lives in the same worker. | Official CF template, simple SSR/SEO with no ISR cache plumbing. |
| Ops metrics | Workers Analytics Engine: per-venue latency, status codes, rows, freshness. `/v1/health`. Cron alert to a Telegram/Discord webhook when a venue has been stale for more than 10 min. | Uses CF services only. |

---

## Repo layout
```
ai-rates/
  wrangler.jsonc (the single root Worker, deployed by Workers Builds) · package.json (bun workspaces) · biome.json · tsconfig*.json
  apps/
    worker/         src/index.ts (fetch + scheduled) · probe/ (Phase 0 geo-probe) · ingest/{collector-do,history-do,hub-do,pipeline}.ts · api/ (Hono /v1) · web/ (SSR pages)
    relay/          (only if needed) Dockerfile + Container class, declared in the root wrangler.jsonc
  py/
    analytics/      pyproject.toml · src/entry.py (WorkerEntrypoint.scheduled) · stability.py · momentum.py · verified_backtests.py
    backfill/       Dockerfile · jobs/{funding_history.py, klines.py} (ccxt, duckdb, pyarrow)
  packages/
    core/           zod schemas (Venue, Market, FundingSnapshot, FundingEvent, Ticker, BookSample) · math/{annualize,spread,slippage,backtest}.ts
    adapters/       src/{http.ts (retry, Retry-After, UA), ratelimit.ts, symbols.ts, venues/<venue>.ts} · __fixtures__/<venue>/*.json
    meta-db/        D1 migrations · typed queries
    ui/             table, venue chip, sparkline, equity chart
  data/             venues.json (logo, referral, fees, points, locationHint) · assets.json (canonical ids, multipliers, class, RWA index)
  plans/
```

## Domain rules (packages/core) — the correctness-critical part
- **Adapter contract:** `VenueAdapter { id, capabilities, listMarkets(), fetchFundingSnapshot(), fetchTickers(), fetchFundingHistory(sym, from, to), fetchOrderbook(sym, depthUsd), fees }`. Every adapter declares `rateUnit` (fraction, percent or bps), `rateBasisHours` (a rate can be quoted per 8h but settle hourly) and `kind` (`predicted`, `settled`, or `index` for DEXs exposing a cumulative funding index, where rate = Δindex).
- **Normalize:** store the raw rate and interval exactly as reported, and derive `rate_per_hour`; `apr = rate_per_hour × 8760 × 100`.
  - Sign convention: positive means longs pay. Record caps, floors and HIP-3 per-asset funding multipliers.
  - Quoting varies by venue:

    | Basis | Venues |
    |---|---|
    | Per interval | most CEXs |
    | Per hour | Hyperliquid, dYdX, Extended, Reya |
    | Normalized to 8h | GRVT `funding_rate_8h_curr`, Paradex `funding_rate_8h` |
    | Quoted period ≠ paid period | Phoenix (quoted per 24h, paid hourly), Paradex `funding_period_hours` |

  - Fixed-point values: Nado `x18`, Bluefin `e9`, Velocity quote/base ÷ oracle TWAP.
  - Interval fields use different units: Bybit minutes, KuCoin ms, Gate seconds, Variational seconds, edgeX minutes, Bitget hours.
  - Timestamps: Aevo uses nanoseconds.
  - OI is reported in base, contracts or USD, so convert everything to USD.
  - **Intervals change** (Binance/Bybit/OKX/Pionex shorten 8h→4h→1h at the cap). In backtests, work out the interval from the gaps between settlements, not from current metadata. Binance `fundingInfo` omits symbols still on the default 8h.
  - Reya reports long and short funding separately.
  - Collateral basis differs (USDT vs USDC vs USDe on HyENA), so show the quote asset.
  - Orderly brokers (WOOFi Pro etc.) share one book, so dedupe.
  - Lighter's funding-rates response includes other venues' rows, so filter by exchange.
- **Predicted vs settled:** Binance `lastFundingRate`, Bybit ticker `fundingRate`, Gate `funding_rate_indicative`, KuCoin predicted, Orderly `est` (8h rolling average) and edgeX forecast are all *predicted*. OKX `nextFundingRate` can be null depending on `method`. Paradex `funding/data` is continuous samples, not settlements. The screener shows predicted APR and also ranks by 24h/7d settled averages. Stability uses settled events only.
- **Symbols:** canonical asset id + contract multiplier (`1000PEPE`, `kBONK`, `XBT`→BTC). The multiplier affects price, not the funding rate. HIP-3 markets are `dex:SYM`, and each HIP-3 dex counts as its own venue. RWA/TradFi pairs must share the same underlying index, not just the ticker.
- **Spread:** apply the filters (OI, volume, staleness, venue) to each leg before taking max/min. No same-venue pairs. Outlier guards: APR cap flag, cross-venue z-score, stale leg (older than 3× interval) excluded.
- **Backtest math:**
  - `qty = size / entry_mark`, the same on both legs.
  - Each settlement cashflow = `qty × mark_at_settlement × rate` (negative for the long leg, positive for the short).
  - Sum each leg's cashflows at its own settlement times, with no resampling or forward-fill. Bucket by UTC day. A gap is flagged, never counted as zero.
  - Costs: 4 taker fills (entry and exit, both legs) plus 4 book-walk slippage estimates. The UI labels the "current book" assumption.
  - Basis PnL `qty × (Δmark_long − Δmark_short)` is shown separately. The headline is "funding net of costs".
  - Definitions shown in the UI: size is per leg; capital = 2 × size / leverage; win rate = % of days with positive net funding; payback = one-time costs / average daily net (shows "never" if ≤ 0).
- **Price arb:** use best bid/ask from book tickers (not mark price). Reject pairs whose timestamps differ by more than about 2s. Net of taker fees on both legs. Executable size comes from depth samples.

---

## Phased delivery (2–3 devs)

### Phase 0 — Foundations and risk spikes (week 1)
> **Status 2026-09-11:** done. Deployed probe: Binance, BloFin, Pionex and Bitget are blocked from every Cloudflare location, so their egress route is an open decision. Bybit, MEXC, Orderly and Extended work when pinned to `apac-ne`. Python Workers on Free run numpy but not pandas. Container and Pipelines spikes were skipped because the account is on Workers Free. Results and the plan changes Free forces are in [`phase0-report.md`](phase0-report.md); referral and legal research is in [`phase0-referrals-legal.md`](phase0-referrals-legal.md).

1. Scaffold the Bun monorepo: biome, tsconfig, CI (lint, typecheck, `bun test`, `uv run pytest`, `wrangler deploy --dry-run` per app).
2. **57-venue catalog + geo-probe:** a throwaway worker hits every venue's public funding/ticker endpoints four ways: from a cron, from DOs hinted `apac-ne`/`weur`/`enam`, and from Containers pinned to APAC/WEUR. For each, record status, `cf-ray` colo, WAF/CloudFront challenges, bulk-endpoint availability, history lookback and rate-limit weights.
   - Output: `data/venues.json` with a fallback-ladder rung per venue.
   - **Decision gate:** if Binance/Bybit/dYdX only work via rung 3 (relayed) or 4 (non-CF proxy), bring that choice to you before Phase 1.
   - Also confirm: thin fetch adapters load within the Worker's 1s startup limit (don't use ccxt-JS; it pulls the whole library).
3. Spikes:
   - Python Worker cold start with numpy/pandas.
   - Pipelines → R2 Iceberg → R2 SQL query round trip.
   - Size estimate for VenueHistoryDO.
4. **Start referral/affiliate applications** for all venues (approval takes weeks). Legal checklist: exchange ToS on redistributing market data (top 15), affiliate disclosure, geo-gating referral CTAs by `request.cf.country`.

### Phase 1 — Ingestion core + first 10 adapters (weeks 2–4)
> **Architecture change (2026-09-12, decided after Phase 0):** Workers Free can't hold Phase 1 (Durable Objects there cap at 100k rows written/day and 5 GB; bulk responses blow the 10ms CPU limit). So **collection and storage run on the team's hklab server** (Hong Kong egress, 16 cores / 28 GB): a Bun collector container plus the existing TimescaleDB instance. The Cloudflare Worker stays as the public edge for Phase 2+. From Hong Kong every Phase 1 venue is reachable, and so are Binance/Bybit/Bitget (BloFin still 403), which removes the Cloudflare geo-block problem for the collector. The DO-based ingest design above is superseded for ingestion.

- **`packages/adapters`:** one `VenueAdapter` per venue with pure, fixture-tested parsers; `fetchSnapshots` (bulk current funding + mark/index/OI/volume) and `fetchFundingHistory` (settled payments).
  - Shared JSON client: request spacing, jittered exponential backoff honoring `Retry-After`, per-venue circuit breaker.
- **`apps/collector`** (Bun, Docker on hklab):
  - One `VenueLoop` per venue on a 60s wall-clock cadence (no overlapping cycles, per-cycle timeout).
  - One `HistoryLoop` per venue that pulls settled funding for markets due a settlement (**settled events recorded from day 1**).
  - `CollectorStatus` health endpoint on `127.0.0.1:20090`.
- **`packages/db`:** SQL migrations (TimescaleDB hypertables `funding_snapshots` with 1-day compression and 30-day retention, `funding_events` kept indefinitely, `markets`, `venues`, `collector_runs`) and a migration runner.
- **Storage:** database `vaultdeck` (role `$AIRATES_PG_ROLE`) on the existing `timescaledb_container`, to be moved onto the 466 GB NVMe mounted at `/srv/airates-data` via a tablespace. The mount and the compose volume need sudo on hklab. Integration tests use their own schema, `airates_it`, in `vaultdeck` over an SSH tunnel.
- **Adapters:** Bybit, OKX, Gate, MEXC, **KuCoin**, **Aster**, Hyperliquid (core + HIP-3 dexes via `perpDexs` / `metaAndAssetCtxs{dex}`), dYdX v4 indexer, Paradex, Lighter. KuCoin and Aster replace Binance and Bitget; Binance, BloFin, Pionex and Bitget are deferred by decision.
- **Deploy:** `deploy/hklab/deploy.sh` streams the repo allowlist, builds on the server, and runs `docker compose` on the `postgres_postgres` network. Runbook: `deploy/hklab/README.md`.

> **Status 2026-09-12:** built and verified end to end from a dev machine against `airates_test`.
> - **Coverage:** 20 venues and ~4.8k markets per cycle (Bybit 829, OKX 478, Gate 970, KuCoin 682, Aster 571, MEXC filling toward ~1.2k, Hyperliquid 178, Lighter 217, dYdX 78, Paradex 63, HIP-3 xyz 104 / para 26 / io 6 / mkts 4).
> - **History:** settled-funding sweeps running (e.g. Aster 27k and Bybit 13.6k events on the first sweep).
> - **Verified:** zero 429s after putting Hyperliquid core and HIP-3 on one shared rate-limit group; clean SIGTERM shutdown in ~4s.
> - **Tests:** 179 passing.
> - **Findings:**
>   - The HIP-3 dexes cash, flx, hyna, vntl, km and abcd currently list only delisted assets, so they return 0 markets.
>   - MEXC coin-settled contracts need USD contract sizing.
>   - dYdX BTC funding is often exactly 0 (within the clamp band).
> - **Production:** live on hklab since 2026-09-12 (`airates-collector`). The first check showed all 20 venues fresh, 0 failed runs, ~4.2k markets, 70 MB RAM. The same day the collector moved from `airates` to the owner-provided database `vaultdeck`/`$AIRATES_PG_ROLE`, with row counts verified identical.
> - **Phase 2 access (ready 2026-09-12):** Cloudflare reaches Postgres directly through Hyperdrive config `airates-vaultdeck` (ID `7c04838b33a6423d8a195fafab6d101f`).
>   - TLS was enabled on the instance (reload only).
>   - A private CA was created and uploaded to Cloudflare.
>   - Connections use `sslmode=verify-full`, checked from the internet (TLSv1.3).
>   - Not yet enforced for other clients. Details in `deploy/hklab/README.md`.
> - **Storage (done 2026-09-12):** the NVMe is mounted at `/srv/airates-data`, and `vaultdeck` was moved into tablespace `airates_nvme` (143 MB at move time; 434 GB free).
> - **Background jobs (fixed 2026-09-12):** the shared instance had 16 TimescaleDB workers for 17 databases, so `vaultdeck`'s compression and retention policies had never run. Raised to 24 workers / 48 worker processes (3-second restart); the policies now run successfully. **Phase 1 complete.**

### Phase 2 — API + screener + exchange/asset index (weeks 3–6)
> **Status 2026-09-12: live** at https://airates.jobhesk.workers.dev (noindex until launch).
> - **As built:**
>   - The collector maintains `market_latest`, `market_funding_stats` (24h/7d time-weighted settled APR) and `screener_pairs()` in `vaultdeck` (migration 002).
>   - The Worker reads them through Hyperdrive `airates-vaultdeck` with Postgres.js.
>   - Pages are server-rendered HTML strings plus a small ticking script, instead of React Router: Workers Free allows 10 ms CPU per request, and the deploy has no build step.
>   - The edge cache (Cache API) answers repeat requests in ~50 ms; uncached responses take 0.2–1.6 s.
> - **Shipped:**
>   - Pages: `/`, `/screener`, `/markets`, `/markets/exchange/:venue`, `/markets/asset/:asset`.
>   - JSON: `/v1/health`, `/v1/screener`, `/v1/exchanges[/:venue]`, `/v1/assets/:asset`, `/v1/venues`.
>   - Design: green-bar sheet with the "spread rail" (signed log scale in multi-asset tables).
>   - Footer with source, not-financial-advice and not-affiliated notices; `robots.txt` disallows all; the probe moved to `/probe`.
> - **Deferred from this phase:**
>   - `/trade` referral hub: waits on referral approvals and written data consent.
>   - SEO work (sitemap, canonicals, per-asset copy): not useful while noindex.
>   - Rate-limiting binding and Turnstile: **both shipped 2026-09-13** — the API gate, the page-side clearance flow, and the `ratelimits` binding (`simple.limit` 20 / `simple.period` 60; the period accepts only 10 or 60). The limiter is **per-Cloudflare-location, not global**, so it bounds one client hammering one colo rather than a distributed caller, which is why the challenge carries the real load. See the Phase 3 entries below.
>   - Per-row "source · updated" stamps: currently in the footer only.
> - **Data quality (2026-09-12, measured against live rows):**
>   - **Fixed — extreme funding.** `screener_pairs` takes `p_max_abs_apr`, defaulting to 1000% APR; `?extremes=1` lifts it and the screener form exposes it. 121 of 5397 live markets sat beyond ±200% APR and 10 beyond ±1000%, led by Gate and MEXC microcaps; open interest alone didn't filter them (STORJ_USDT held $1.5M OI at -1595%). Top spread at the default filters fell from 1356% to 521%. Migration 003.
>   - **Fixed — contract scale.** Prices were stored exactly as venues quoted them, so `1000PEPE` / `kPEPE` / `10000CAT` markets sat 1000–10000× above the unscaled venues for the same asset: PEPE marked 0.00000327 on eight venues and 0.00327 on six. 55 markets, ~12k snapshot rows. `perUnitPrice` divides by the multiplier at record time; migration 004 backfills. Funding rates are fractions and were never affected, and `open_interest_usd` was always correct because adapters derive it from the venue's own contract price.
>   - **Fixed — one asset split across two tickers.** `NG`/`NATGAS`, `WTI`/`CL`, `GOLD`/`XAU` and `SILVER`/`XAG` are the same underlying on different venues — confirmed by mark price agreeing to ~0.3% — so they never paired with each other. Now aliased in `parseVenueSymbol`.
>   - **The "ticker collision" premise was wrong.** `CL` ($96.15–96.41 across 7 venues), `BZ` (~$100.4 across 6) and `NG` (~2.94) agree on mark price everywhere, so those spreads are real, not identity errors. Real collisions are rarer and look different: `gate:CAT_USDT` at $817.80 is not the CAT memecoin, `hl-para:STX` at $830.42 is not Stacks, `JPY` is quoted both ways (0.0065 vs 153.45), and `KR200` disagrees by 1371×. `SPX` (~$0.486, the SPX6900 token) must never be merged with `US500` (~$7,650, the index).
>   - **Open — collision guard.** Prefer a mark-agreement check inside `screener_pairs` (legs of one asset must price within a tolerance) over a curated identity map: it is data-driven and catches CAT, STX, JPY and KR200 at once. Only workable now that marks share one scale.
>   - **Fixed — Aster open interest.** All 571 markets reported none, so the default OI filter dropped the whole venue. Binance-style APIs expose it only per symbol (`openInterest` rejects a missing symbol, `ticker/24hr` carries none), so each cycle now refreshes a slice of 120 symbols, re-reading each about every 5 minutes: 124 requests in 16.3s of a 60s cycle, at request weight 1 of ~2400/min. Contracts are priced with the venue's own per-contract mark, before the store rescales prices per base unit.
>   - **Fixed — MEXC coverage after a restart.** MEXC only emits a market once it knows the settlement interval, and that cache was memory-only, refilling at exactly 40 per cycle (measured across three restarts: 160 markets after 4 cycles, 400 after 10, 480 of 1191 after 12). For roughly half an hour most of MEXC was missing from the screener. Adapters can now declare `warmUp`, and the collector seeds them from `activeMarkets` before the first cycle: the first cycle after the deploy emitted all 1191 markets in 4.7s.
>   - **Fixed — stale-venue alerts.** A venue that stops collecting was silent: the site keeps serving its last rows until they age out. The collector now checks `CollectorStatus` every minute and posts transitions (venues newly stale, and full recovery) to `ALERT_WEBHOOK_URL`, Slack- or Discord-shaped. Only transitions are sent, so a partial outage produces a couple of messages rather than one a minute. Inert until the variable is set.
>   - **Fixed — geo-probe cron retired.** It answered its Phase 0 question and the collector runs from hklab now, so the hourly trigger is gone (`crons: []`, which is what deregisters it; dropping the block can leave the trigger registered). The routes and `ProbeDO` stay, so `POST /v1/probe/run` still works if egress ever needs rechecking. Deleting the class outright would need a `deleted_classes` migration and destroys its stored data, so that is left as a deliberate choice rather than a side effect.
- `/v1/screener`, `/v1/assets/:sym`, `/v1/exchanges/:venue`, `/v1/health`. Cache headers: screener `s-maxage=15, swr=60`, pages `s-maxage=60`, ETags. Rate-limiting binding on the API.
- Pages: `/screener`, `/markets`, `/markets/exchange/$venue`, `/markets/asset/$sym`, `/trade`. Dark terminal theme, venue logos, long/short chips, "Backtest" CTA.
- Every row/page shows "Source: {venue} public API · updated {ts}". Disclaimer text ("not financial advice / not affiliated").
- SEO: SSR tables, text summary per asset page, sitemap index, canonical tags (noindex query-param variants of `/pair`).

### Phase 3 — Backtester (weeks 6–7)
> **Status 2026-09-12: history deepened, engine and API live; the page is what's left.**
> - **The blocker was history depth, not the maths.** `sweepVenueHistory` only ever resumes from the newest stored settlement, so every venue held exactly the 7.1 days since collection began on 2026-09-04. A 30-day backtest was arithmetically impossible.
> - **`backfillVenueHistory` reaches the other way**, asking each venue for the span between a 90-day target and the oldest settlement already stored. Confirmed against live APIs first: aster serves back to 2026-06-14, bybit gives bounded windows 90 days back, okx pages 100 at a time, gate returns 60-day windows despite capping an unbounded call at 90 rows. Paradex publishes continuous accrual samples rather than settlements and excludes itself.
> - **Result:** history went from 7.1 to **89.9 days** within minutes of deploying; 253,610 → 271,907 events on the first round of sweeps, bybit's first sweep alone pulling 8,204 events with no errors.
> - **It runs slowly on purpose:** 20 markets per venue every 5 minutes, because deepening the past shares one rate limit with live collection. Markets that return nothing older are remembered as exhausted so later sweeps spend the budget where it still moves.
> - **The engine** (`packages/core/src/backtest.ts`) replays what both legs actually settled, summing each at its own settlement times — Hyperliquid settles BTC hourly against Bybit's 8-hourly, so nothing is resampled onto a shared grid, and a gap is reported as missed settlements rather than counted as zero funding.
> - **Two limits the data forces, both stated rather than papered over.** Notional is held constant at `sizeUsd`: the exact rule is qty × mark-at-settlement × rate, but of 371,170 stored settlements only dydx's carry a mark, so per-settlement precision would be fiction. Basis PnL needs mark history we don't collect and is left out. Costs appear only when the caller supplies fees — the venue catalog has no fee fields at all, and an invented taker fee would quietly corrupt every net figure.
> - **`/v1/pairs/:asset/backtest`** takes exchange names rather than venue symbols (`?long=dydx&short=hyperliquid&days=7`), resolving each to that venue's deepest market for the asset, capped at the 90 days the backfill reaches, cached an hour.
> - **A bug worth remembering:** the endpoint shipped green on 60 tests and failed on every production call. `settlements()` bound a `text[]`, but Hyperdrive requires `fetch_types: false`, and with type introspection off postgres.js sends an array as the bare string `"dydx,hyperliquid"`. The lesson was the coverage, not the query: `app.test.ts` fakes the DataSource, so no test ever executed the SQL. `data.int.test.ts` now drives it against the real schema with production client options.
> - **Built:** the `/pair/$sym` page — leg picker, result, and a server-rendered SVG equity curve (the site has no chart primitive and the Free plan allows 10 ms CPU). It states its own coverage: when a window has fewer days of settlements than requested, it says so rather than annualizing a hole silently.
> - **Shipped 2026-09-12 — `/rates`, assets × exchanges** (shipped as `/heatmap` and renamed hours later; see the rename note at the end of this entry). The screener answers "widest spread for one asset"; nothing answers "where does this asset's funding sit everywhere at once" without opening a page per asset. Rows are assets, columns are exchanges, cells are funding APR, after orbitperpscreener.com/screener.
>   - **Measured 2026-09-12:** 1,571 assets × 14 venues, 891 assets on 2+ venues, 4,706 populated cells — a grid only ~38% full, so empty-cell treatment is a design problem, not a detail. Orbit renders ~45,000 cells in a virtualized React client; we render HTML strings under 10 ms CPU, so the default is the **top 150 assets by open interest, paged**.
>   - **Timeframes NOW / 7d / 30d / 60d.** The first two exist; 30d and 60d do not exist in any read model and cannot be computed per request (a 60d aggregate scans ~2.4M `funding_events` rows). They become precomputed columns on `market_funding_stats`, ideally via a TimescaleDB **continuous aggregate** over daily buckets — `funding_events` is already a hypertable — rather than an hourly full rescan on an instance shared with 16 other databases.
>   - **Reuse, don't reinvent:** `railScale`/`railPosition` in log mode already compress ±1000% APR onto 0–100, which is the cell-intensity mapping. `aprTone` is sign-only and is not enough. `probe/render.ts` is already a sticky-header matrix with per-value cell classes — copy the technique, not the file.
>   - Full plan: `~/.claude/plans/whimsical-juggling-dongarra.md`. **Note:** it calls the stat-window migration `006_stat_windows.sql`, but 006 is now `max_leverage` and 007 is `leverage_tiers`, both applied in production. The heatmap migration must be **008**; migrations are applied in filename order and recorded by filename, so a reused number would be skipped as already-applied and the columns would silently never exist.
>   - **Built in three slices, riskiest first.** H1 the data foundation, H2 the query/params/`/v1/heatmap` (now `/v1/rates`), H3 the page.
>   - **Renamed `/heatmap` → `/rates` the same day, for three reasons.** (1) In perp trading "heatmap" overwhelmingly means a *liquidation* heatmap (Coinglass, Hyblock) — the very thing Phase 4 discusses and deliberately declines to build — so the name collides with our own roadmap, and renaming later would mean breaking links already in the wild. (2) It was the only nav item named after its rendering rather than its contents: spreads, screener, exchanges and pair are all plain nouns. (3) It captures the actual difference from `/screener` — the screener gives the best *pair per asset*, `/rates` gives *every rate everywhere*. The counter-argument is real and was weighed: Coinglass ships a "Funding Rate Heatmap" with exactly this shape, so the old name was defensible vernacular, not a mistake.
>   - **Only the user-facing surface moved.** URL, nav label, page title, `<h1>` and the hotkey (`m` → `r`, which also frees `m` and is more guessable). The internal identifiers keep the heatmap spelling on purpose — `HeatmapCell`, `parseHeatmapParams`, `heatBucket`, `HEATMAP_VALUE`, `.heat`/`.heat-wrap` and the `hm-p1…hm-n5` classes — because the *rendering technique* genuinely is a heatmap even though the page's name is about the question it answers. That kept the change to 10 sites in 4 files instead of a sweep.
>   - **Both old paths 301 rather than 404**, query string carried through, so a shared `/heatmap?tf=60d` link lands on the view it named. A test pins the status, both locations and the no-query case (no trailing `?`, so the canonical URL stays a single edge-cache key).
>   - **H1 deliberately did not use a TimescaleDB continuous aggregate**, though this plan said to try one first. Measured on the instance rather than assumed: `funding_events` already has compressed chunks and a 12-hourly compression policy, so a 60-day refresh window spans compressed data — the interaction flagged above as most likely to bite; the database shares a background-worker pool with 16 other tenants and already runs nine policy jobs; and the integration-test schema duplicates every policy (jobs 1004–1006 mirror 1000–1002), so a refresh job would be created twice. Instead migration 008 adds `market_funding_daily`, which the collector folds incrementally. No background jobs, portable off TimescaleDB, covered by the existing harness.
>   - **The rollup stores `rate_sum` and `basis_hours_sum`, never a finished APR.** Markets settle on different intervals, so a window has to be `sum(rate)/sum(basis_hours)` divided once at the end; averaging daily APRs would weight a 1-hourly market the same as an 8-hourly one. `refreshDailyFunding` derives its own start from the newest stored day, so an empty rollup builds the whole lookback and an outage heals itself with no separate backfill path.
>   - *H1 verified in production:* migration 008 applied 13:21:22Z; the first run folded the entire 70-day lookback — **344,596 day-rows across 5,336 markets** — inside one scheduling tick, so the day-chunking held in reserve was never needed. The join fills **5,336 of 5,399 live cells**; the 63 without are newly listed markets with no settled history, which render as `–`.
>   - **Cell colour follows `aprTone`, not intuition:** positive funding is red and negative blue, because positive means longs pay and a short collects. Assuming "positive is good, so blue" would have inverted the grid against every other page. An absent market is a dim dash with no tint — ~62% of the grid is empty, and a coloured zero would read as "flat here" rather than "nothing here".
>   - **The grid scrolls on both axes in its own box.** This plan had the header stick to the viewport under the masthead, which is why the masthead was measured at **46px** (`.bar` 22px twice, plus `.bar2`'s `border-top` and `.mast`'s `border-bottom` — not the 44px assumed). But `.sheet-wrap` is `overflow-x:auto` and CSS coerces the other axis to `auto`, which would have anchored the header to that box while the page scrolled past it. Its own scroll box makes the sticky header and first column behave and makes `top:0` correct, so the measurement is what ruled the approach out rather than what the code uses.
>   - **Also de-duplicated the hotkey map**, which this plan called out: it was written in `KEYS` for the status bar and again inside the inline `SCRIPT`, so a key could be advertised and do nothing. `SCRIPT` now derives it from `KEYS`; the heatmap is `m`.
>   - *Measured before trusting it, as this plan insists.* **89,606 bytes at the 150-row default, query 7.7 ms** (`EXPLAIN ANALYZE`, all buffers `shared hit`, existing `market_latest_base` index), cold ttfb 1.5–2.7 s, **cached hits 70–80 ms (`cf-cache-status: HIT`)**. **The 150-row default stands:** `limit=50` cut the payload 59% and was no faster, so rows are not the cost, and `/screener` is *larger* at 112,511 bytes. A first reading suggested the page was not being edge-cached; a controlled comparison against `/screener` and `/markets` disproved it — the earlier figure was a cache-buster racing `ctx.waitUntil(cache.put(...))`, not a caching failure.
> - **Shipped 2026-09-12 — screener sortable columns.** Spread, 7d settled and venues as header links, with `aria-sort` on the active column. Links rather than script, so an order is shareable and keyed by the edge cache.
>   - **Only three of the four sort keys listed at the top of this plan are real.** "Sorting: predicted APR, 24h/7d settled average, stability, OI" overstates what `screener_pairs` returns. **Stability does not exist anywhere** — it is an unbuilt py-analytics deliverable. **Open interest is returned per leg only**, and summing the winning pair's two legs ranks by pair-selection artefact rather than asset depth: 龙虾 shows $20.8M across two venues, above FLOCK's $6.8M across six. Both are omitted rather than shipped as headers that sort by nothing or by an invented number.
>   - The key indexes a fixed set of `ORDER BY` fragments and is never interpolated. Each fragment ends with `asset`, because `LIMIT` over a partial order drops and repeats rows between requests — the `settlements()` bug found during A1. Each carries explicit `NULLS LAST`: `spread_apr_7d` is the difference of two per-leg stats, and though all 653 live pairs currently have one, 63 live markets have no stats row at all, so that is luck rather than a guarantee.
>   - **The ordering test inverts the two orders on purpose** — `base2` has the wider live spread, `base` the wider 7d one — because orders that agreed would pass whichever fragment actually ran. Nothing previously pinned this `ORDER BY`: `data.int.test.ts` only asserted `screener()` resolves, the same unguarded shape that let a broken `text[]` query reach production.
>   - *Verified live:* the three sorts return genuinely different pages — spread `ONE, 龙虾, FLOCK`; settled_7d `STONK, ANSEM, XCN`; venues `BTC, ETH, XRP` — and no `sort` param matches spread exactly, so the homepage ordering is unchanged. `/v1/screener` echoes both `sort` and its canonical `query`, and an unbacked key falls back.
>   - The **rail gradient turned out to be a consistency fix, not a feature**: `.rail-big` already used a long-to-short gradient and only the table rows were flat `var(--dim)`. They now match.
>   - *Debt worth naming:* `data.int.test.ts` now seeds three assets in the shared `airates_it` schema, and two assertions broke today purely because a later fixture seeded rows an earlier test assumed absent. Fixed by giving the null-window assertions their own asset, but that file is accumulating coupling and the next fixture added to it will probably break something again.
> - **Next — leverage-aware capital.** The pair page assumes 1×, so "capital $20,000" overstates what a pair ties up. Both legs must be open at once, so the binding constraint is the **lower venue at the entered size**. Capital is `size × (imr_long + imr_short)`: the legs sit on different exchanges and margin independently, so both margins must be posted. At 1× that is `2 × size`, matching what the page shows today, and under symmetric leverage L it reduces to `2 × size / L`. (An earlier revision of this entry said `max(imr_long, imr_short)`, which is one leg's margin and halves the true figure.)
>   - **Why a single number won't do:** Bybit's headline 150% leverage on BTCUSDT holds only to $300k notional and falls to 100× by $2M; OKX starts at 100× and is 40× by its fourth tier; MEXC advertises 500×. A flat max overstates usable leverage by 3–5× at realistic size.
>   - **Spike, done 2026-09-12 — the ladders are not uniform, and that is the whole risk:**
>     - *Free in calls we already make:* Bybit `maxLeverage`, Gate `leverage_max`, KuCoin `maxLeverage`, Hyperliquid `maxLeverage` — and Hyperliquid's full ladder too, since `meta` returns `marginTables` beside `universe`.
>     - *Enumerated, per-symbol call:* Bybit `/v5/market/risk-limit` (35 tiers on BTCUSDT), OKX `/public/position-tiers` (99), Gate `/risk_limit_tiers` (19), KuCoin `/contracts/risk-limit/{sym}` (12).
>     - *Parametric, not a list:* MEXC `riskBaseVol`/`riskIncrVol`/`riskIncrImr` — a formula, currently degenerate (one level, flat 500×).
>     - *Unit trap:* OKX tier bounds are in **contracts**, not USD (`ctVal 0.01 BTC`, so `maxSz 1000` = 10 BTC ≈ $780k). Gate, Bybit and KuCoin bound in quote notional. Converting needs `ctVal × mark`, the same normalisation already done for open interest.
>     - *Flat:* dYdX (`initialMarginFraction` 0.02 → 50×). *Absent:* Aster, Paradex, Lighter.
>   - **Storage:** one uniform table `market_leverage_tiers (venue_id, venue_symbol, tier, lower_notional_usd, upper_notional_usd, imr, mmr, max_leverage, fetched_at)`. Parametric ladders are expanded to rows at ingest and contract bounds converted to USD, so the read path is a single `WHERE size BETWEEN lower AND upper` on both legs. Tiers change rarely, so refresh daily, not per cycle.
>   - **Order — thin vertical slices, riskiest unknown first, each one shippable:**
>     1. **A1 — flat max, zero new requests. Shipped 2026-09-12.** Capture `maxLeverage` already present in the bulk responses (Bybit, Gate, KuCoin, Hyperliquid) into a nullable `markets.max_leverage`. Pair page shows `2 × size / min(long, short)`, `–` where unknown. *Check:* capital changes on a BTC pair and stays honest where a venue is silent.
>        - Migration `006_max_leverage.sql`; the four adapters set `maxLeverage` from calls the collector already makes every cycle, so the request budget is unchanged. `markets.max_leverage` uses `COALESCE(EXCLUDED, existing)` like `interval_hours`, so a response that transiently omits the field does not erase a known value.
>        - The read path joins `markets` from `market_latest` rather than denormalising a second copy of the column, keeping one source of truth. Capital is the lower of the two venues' maxima: a pair cannot run at 100× on one leg if the other caps at 10×. A venue that publishes nothing drops the whole pair to unleveraged rather than borrowing its partner's figure — the same rule as absent fees and absent marks.
>        - *Verified in production on the first post-restart cycle:* gate 970/970 markets, bybit 829/829, kucoin 682/682, hyperliquid 178/178 plus every HIP-3 dex. Aster, dYdX, Lighter, MEXC, OKX and Paradex stay NULL, which is exactly A1's declared scope (dYdX is flat 1/IMF and MEXC/OKX are parametric or contract-bound — both B3).
>        - *Incidental fix:* `settlements()` ordered only by `settled_at`, so markets settling on the same tick came back in planner-dependent order. The order is now total (`settled_at, venue_id, venue_symbol`); an integration test had been passing on luck.
>     2. **B1 — one venue end to end. Shipped 2026-09-12.** Migration + tier table + Bybit's enumerated ladder + IMR capital on the pair page. *Check:* an integration test in `data.int.test.ts` (production client options) and a hand-computed tier boundary — $250k vs $2M on BTCUSDT must give different capital.
>        - **The sweep is whole-venue, not a per-symbol rotation — probing the API changed the design.** `/v5/market/risk-limit` with no `symbol` returns *every* symbol, paginated; it ignores `limit` and yields ~15 symbols a page, so the entire linear book costs ~56 requests rather than one per market. It therefore runs daily as a single `PeriodicTask` and needs no budget. **This is the third collector sweep but the first with no per-cycle budget, so it does not multiply the rotation pattern.** The consolidation I wanted before B3 now only matters for venues whose tiers are per-symbol only (Gate, KuCoin, OKX) — check each venue for a bulk endpoint before assuming a sweep is needed.
>        - **Half-open bounds `[lower, upper)`.** Bybit publishes only an upper bound per tier, so each tier inherits its floor from the one below. Verified in production on BTCUSDT: tier 1 `[0, 300k)` imr 0.0066 (150×), tier 2 `[300k, 2M)` imr 0.01 (100×), tier 3 `[2M, 2.6M)` imr 0.0111 (90×). The plan's boundary check passes — $250k lands in tier 1 and $2M in tier 3, since $2M is excluded from tier 2's open upper bound.
>        - **The top tier's bound is a real cap, not infinity.** Bybit will not open a BTCUSDT position above $1.2bn at all, so a size above every tier resolves to *no* tier and the page says the venue would refuse it, rather than rounding down into the top band and quoting capital for a trade that cannot exist. A null upper bound is reserved for venues that genuinely publish no cap.
>        - **A ladder with an unreadable tier is dropped whole**, since keeping the rest would silently stretch a neighbouring band across the gap. Tiers **upsert then prune** (never delete-then-insert: the worker reads this table continuously), and an **empty sweep is treated as a failed request**, so a timeout can never erase good ladders.
>        - *Verified in production:* migration 007 applied 10:53:33Z; first sweep at 10:57:39Z stored **25,318 tiers across 873 markets** in ~6s.
>        - **No live pair reaches the tiered path yet.** Both legs need a ladder, pairs span two exchanges, and Bybit is the only venue with `fetchLeverageTiers` — so until B2 lands OKX, every pair still renders the A1 headline path. B1's value in production is the stored ladder and the tested read path, not a visible change.
>     3. **B2 — normalisation proven on the hard case. Shipped 2026-09-12.** Add OKX, converting contract bounds via `ctVal × mark`. *Check:* OKX tier 1 resolves to ≈$780k, not $1,000. This is the slice most likely to be wrong, which is why it comes before fan-out.
>        - **The unit trap, confirmed.** OKX bounds tiers in **contracts**, not notional. BTC-USDT-SWAP is `ctVal 0.01 BTC`, so tier 1's `maxSz` of 1000 is 10 BTC — about **$772.6k** at a 77,263.6 mark, not $1,000. Three orders of magnitude, which is exactly why this slice came before the fan-out.
>        - **Inverse markets need different arithmetic, and `ctValCcy` is the discriminator.** "USD" (15 of 479 markets, settled in the base coin) already sizes a contract in dollars: BTC-USD-SWAP tier 1 ends at `2000.1 × $100` = $200,010 with the mark playing no part. Applying the mark, as the 464 linear markets require, would overstate that band roughly 77,000-fold. A linear ladder with **no** mark is dropped rather than converted against a missing price.
>        - **Sweep shape set by probing, not assumption.** `position-tiers` refuses to answer without `instFamily` (code 50015) and caps families at five per call (code 50025) — and all 479 perps are their own family. A full sweep is instruments + mark-price + 96 tier calls ≈ **98 requests, daily**, the same order as Bybit's 56. **So B2 needs no budgeted rotation either** and the consolidation question stays deferred rather than forced. It holds OKX's shared client ~12s a day, which can delay one snapshot cycle.
>        - **Bounds made half-open, as on Bybit.** OKX publishes inclusive `[0, 1000]` then `[1000.01, 5000]`; carrying that gap through would leave a **~$7.78 hole** on BTC that resolves to no tier and would render as "the venue will not open this". Each band now ends where the next begins; the top band keeps its own `maxSz` as a real cap.
>        - **Trade-off — contracts are converted to USD at ingest**, per this plan, so stored bounds drift with the mark between daily sweeps. A few percent only matters for a size sitting that close to a boundary, and the IMR steps either side are coarse. Read-time conversion would be strictly more accurate (the page already holds a current mark) but would make the table non-uniform and rework B1's tested read path.
>        - **No collector change was needed:** `main.ts` keys off `adapter.fetchLeverageTiers`, so adding the hook is the entire wiring. That was B1's design paying off.
>        - *Verified in production:* first sweep 11:38:58Z stored **41,153 tiers across 469 of 479 markets**. BTC-USDT-SWAP tier 1 came out `[0, $772,801.73)` at imr 0.01 — the ≈$780k the check demanded — with tier 2 starting on exactly that figure, so the bands are contiguous. BTC-USD-SWAP (inverse) tier 1 is `[0, $200,010.00)`, exactly `2000.1 × $100` with no mark applied. The stored ceiling implies a sweep-time mark of ~77,279.6 against 77,308.4 a few minutes later — the ingest-time drift above, visible in the data.
>        - **The tiered path is now reachable on the live page.** bybit/okx is the first pair with a ladder on both legs. Before B2 it read `capital $20,000 across both legs, unleveraged` at $10k — OKX publishes no headline figure, so A1's rule correctly dropped the whole pair to 1×. After: **$166 at 120.5×** ($10k), **$10,000 at 100×** ($500k), **$25,000 at 80×** ($1M), each matching a prediction computed by hand from the two stored ladders before looking.
>        - **A defect the 469-of-479 gap exposed, since fixed.** The ten missing markets were two exact groups of five — `POSITION_TIERS_PER_CALL` — so two batch calls had failed. Probing reproduces it: those families all return tiers individually, but rapid successive calls earn code 50011 "Too Many Requests", which **OKX returns inside an HTTP 200**, so the transport's retry and circuit breaker never see it. Two faults, the second worse: the `catch` swallowed a failed batch with no retry, count or log; and `replaceLeverageTiers` prunes every row older than the sweep, so a *partial* sweep **deletes the ladders of markets it never read**, turning a transient rate limit into lost data. Fixed by retrying batches with backoff, returning `{tiers, complete}` from the hook, pruning only on a complete sweep, and logging `(partial sweep, nothing pruned)` otherwise. *Verified:* the next sweep covered **479 of 479 markets (42,053 tiers) at 12:03:38Z with no partial-sweep line.*
>     4. **B3 — fan out. Shipped 2026-09-12.** Gate, KuCoin, MEXC, Hyperliquid, dYdX. **The budgeted rotation this entry assumed was needed for only one of the five.** Probing each venue first — the habit B1 and B2 established — found that three need no new requests at all.
>        - **Free, from calls the collector already makes:** Hyperliquid (`meta.marginTables`), dYdX (`initialMarginFraction` in `perpetualMarkets`), MEXC (the ladder and base rates in the bulk contract detail).
>        - **Gate is bulk after all.** `risk_limit_tiers` with no `contract` answers for every contract, and `offset` counts **contracts, not rows**, so the whole venue costs ~10 calls. Only the bulk form carries a `contract` field — the per-contract form omits it, which would leave every ladder unattributable.
>        - **KuCoin is the only genuine per-symbol sweep** (its bulk form answers `404000`): ~680 calls at 150ms, about 100s once a day, which holds its shared client long enough to delay a snapshot cycle or two.
>        - **Units measured, not inferred.** MEXC's `maxVol`/`riskBaseVol` and KuCoin's `maxRiskLimit` are all quote notional. MEXC proves itself internally: on every CUSTOM contract `riskBaseVol` equals that contract's top `maxVol`, and read as contracts BTC's first band would be ~$386k at 500×. For KuCoin, `initialMargin` 0.008 is exactly 1/125, and read as lots its first band would be a $19m position at 125×. *A near-miss worth remembering:* MEXC's TFUEL reads $400 as notional and $414 as contracts — indistinguishable only because its contract is worth about a dollar, so a single sample would have "confirmed" either answer.
>        - **dYdX is not a flat 50× venue.** Majors margin at 0.02 but LINK sits at 0.1 (10×), so the fraction is read per market. One unbounded tier each, since dYdX publishes no position cap.
>        - **Hyperliquid's real coverage is 34 of 234 core assets, and that is correct.** It publishes tables 50–56 but its assets also reference 3, 5, 10 and 20 — ids nobody publishes, where **the id is the flat leverage** (table 3 → 130 assets all at 3×, table 5 → 63 at 5×). Those 198 assets are untiered, and A1's `markets.max_leverage` already carries their cap, so they keep the honest headline path. **Correction:** B3's commit said this "applies to every HIP-3 dex too", which overstated it — each HIP-3 dex publishes only table 50, so 4 assets resolve on hl-xyz and 1 on hl-mkts. Merging core's tables in would resolve none of the missing ids and could only mis-attach a ladder, so it is deliberately not done.
>        - **MEXC has two forms, and this plan's original note was right.** 157 of 1192 live contracts are `riskLimitMode: "CUSTOM"` with an enumerated ladder; **1035 are `"INCREASE"`**, which enumerates nothing — a base rate and a cap, with `riskLevelLimit: 1` meaning exactly one band. **Correction:** B3's commit dismissed the "a formula, currently degenerate" note above as "simply out of date"; it is accurate for 87% of the venue and out of date only for the CUSTOM minority. The INCREASE form shipped separately and took MEXC from 157 to **1187** markets. Five contracts (ALIGN, STONKBROKER, QUID, CLAN, PISTACIO) publish `riskBaseVol: 0` and are skipped rather than handed an invented cap.
>        - *Verified in production:* **~89,000 tiers across 4,318 markets**, every sweep complete with no partial-sweep line — bybit 25,318/873, okx 42,053/479, gate 14,824/981, kucoin 5,451/682, mexc 1,473/1,187, dydx 78/78, hyperliquid 68/34, hl-xyz 4/4.
>        - *Five pair figures computed by hand from the stored ladders before looking, all exact:* gate/bybit **$116 at 172.4×**, kucoin/bybit **$146 at 137.0×**, dydx/bybit **$266 at 75.2×**, mexc/bybit **$86 at 232.6×**, hyperliquid/bybit **$316 at 63.3×**. KuCoin at $500k gives **$10,000 at 100×**, confirming both legs step up together, and ZEC on mexc/bybit reads **$243 at 82.3×** — a market with no ladder at all before the INCREASE work.
>     5. **B4 — the three gaps. Shipped 2026-09-12.** Aster, Paradex and Lighter publish nothing usable, so the venue catalog carries a conservative hand-curated `maxLeverage: 10`.
>        - **It lands in `markets.max_leverage`, never in `market_leverage_tiers`.** A curated number is a headline figure and a judgement call; the tier table stays reserved for ladders a venue actually publishes. The pair page then labels it "(small size)" of its own accord, with no special casing.
>        - **Erring low is deliberate.** Capital is `2 × size / L`, so too small a figure overstates what a pair must post rather than flattering it, and all three venues really offer more than 10×. A figure a venue reports itself always wins, which the store integration test pins down.
>        - *Verified in production:* aster 571/571, lighter 217/217 and paradex 63/63 markets carry it, and all three paired against bybit read **`capital $2,000 across both legs at 10× (small size)`** where they previously read `$20,000 unleveraged`.
>   - **Ordering rationale:** A1 ships value on day one with no new requests; B1 proves the schema on the simplest enumerated ladder; B2 attacks the unit conversion while the blast radius is one venue; only then does B3 multiply the request load. Each slice leaves the pair page correct, never half-migrated.
> - **Shipped 2026-09-12 — funding stability and momentum (S1).** The top of this plan lists "stability" as a screener sort key and the Phase 3 text below assigns it to an hourly `py-analytics` job. **That substrate does not exist** — `wrangler.jsonc` has no `d1_databases` and no `r2_buckets`, and `py/` contains only `spike` — so this takes the same correction already recorded for `py-backfill` and VenueHistoryDO: the collector computes it in SQL from migration 008's `market_funding_daily`, a sum over at most ~30 small rows per market.
>   - **The definition was pre-registered in migration `009_stability.sql` before any score was looked at.** This plan's own Phase 4 warning — pre-register the definition "or the study will find whatever it is asked to find" — applies just as much to a ranking metric as to a regression. `stability_30d = (dominant_days + k/2) / (charge_days + k)`, `k = 10`, over days whose `rate_sum` is non-zero, where `dominant_days` is the larger of the positive- and negative-day counts.
>   - **Three definitions were tried and rejected first, and each failure is the reason for one clause of the final one:**
>     - *Coefficient of variation* put bybit BTCUSDT (holds its sign 97% of days) only 2.4× from 1000BONKPERP (flips two days in three), and explodes toward infinity whenever the mean sits near zero, which is common. A carry trader is asking "will this leg keep paying me", not "how wide is the daily swing" — so sign consistency, not dispersion.
>     - *Agreement with the sign of the mean* has a knife-edge at zero: a balanced market has a mean of exactly 0, `sign(0)` matches neither direction, so no day agrees and it scores the floor — while the same market at +ε scores 0.5. The **dominant** sign has no such discontinuity and needs no zero-mean special case.
>     - *An `|mean| >= 0.5%` activity floor* scored only 75% of the book and discarded bybit AAVEPERP, which charges on all 31 days at 0.341%. Small but relentless carry is exactly what this should reward, so it gates on activity and never on the size of the mean.
>   - **Shrinkage rather than a hard minimum-day count.** dydx AAVE-USD charges on 6 of 30 days and agrees with itself on all 6, so raw it scores 1.00 and outranks BTC's whole month. At `k = 10` it reads 0.688 against BTC's 0.854, and the sparse market keeps a usable figure instead of being discarded.
>   - **The usable scale is 0.5–0.878, not 0–1, so the read path must never present it as a percentage.** Dominant-sign cannot fall below half, making 0.5 a mathematical floor (*verified:* `below_half = 0`, `min 0.5000`), and shrinkage caps the best attainable score at `(n+5)/(n+10)` — 36/41 = 0.878 over a 31-day window (*verified:* `max 0.8780`). A reader who expects the best market to approach 1.00 will misread every figure on the page.
>   - **Zero-funding markets score nothing rather than scoring perfectly.** Charging days are excluded from numerator and denominator alike: 85 markets charge nothing across all 31 days — the tokenised-equity perps (bybit IBMUSDT, aster MSFTUSDT, okx HIMS-USDT-SWAP) — and a naive sign test hands them a **perfect 1.00**, ranking dead instruments top.
>   - **A correction to my own "self-healing" claim in the H1 entry above.** That entry says `refreshDailyFunding` derives its start from the newest stored day, so an outage heals itself with no separate backfill path. It heals **forward only**, while `backfillVenueHistory` deepens history **backwards** — so day-rows arriving behind the watermark would never be folded and the 30-day window would quietly thin as the backfill ran. A stability integration test caught it; inspection had not. The fold now always covers the full lookback, measured at **606 ms** for all 70 days, which is why no incremental resume is worth the risk it carries.
>   - *Verified in production:* migration 009 applied 14:05:10Z, and the first hourly run logged **344,596 day-rows folded, 5,336 markets updated, 5,251 scored** at 14:10:15Z — the 85 unscored being exactly the zero-funding perps above. Every value computed by hand before looking matched exactly: bybit BTCUSDT **0.854** (31 days, momentum −2.95), dydx AAVE-USD **0.688** (6 days, +1.42), bybit AAVEPERP **0.659**, bybit 1000BONKPERP **0.634**, bybit SOLUSDT **0.585**, mexc 1000BONK_USDT **0.512**. The distribution spans eight buckets (415/434/361/410/475/773/703/1680), so the metric discriminates rather than piling up at one end.
>   - **`momentum_30d` splits on the calendar, and migration 009's own header overstates it.** That header says "the last 7 **charging** days" and that the result is NULL "when every charging day falls inside the last 7". The shipped SQL (`store.ts:336-337`) splits on a **calendar** boundary, and `recent - prior` is NULL when **either** side is missing. Both halves are real: of 553 scored markets with no momentum, **400 have no charging day in the last 7 days** (funding stopped) and **153 have none before it** (too new, or too sparse), with **0 unexplained**. My first two explanations each accounted for one half only, and the counts refusing to add up is what surfaced the second.
>   - **Those 400 are correct behaviour, not a collection gap — measured rather than assumed charitably.** All 400 are still fresh in `market_latest` (`absent_from_latest = 0`) and 398 currently read `apr = 0`. Across that class, 3,517 day-rows carry `settlements > 0` with `rate_sum = 0` and **`days_with_no_settlement = 0`**: funding events are landing on schedule and settling at exactly zero.
>   - *Debt found on the way, and not an S1 defect:* **two markets publish a live predicted APR while settling exactly zero** — gate IBM_USDT (42.8 APR, three settlements a day at zero since 2026-09-02, last non-zero charge 2026-08-26) and bybit ETHBTCUSDT (26.8 APR, the same shape since 2026-08-31). The screener will advertise carry that neither has paid in nearly two weeks. Predicted-versus-settled divergence deserves its own check across the book.
>   - **Provenance of the NUL byte fixed in `4cfb37e`, established only after that commit was pushed:** it entered in Phase 1's `1dcb5d5` and had been in `store.ts` ever since, making `grep` treat the file as binary and silently return nothing. Its cost was a false belief rather than a runtime fault — it produced the wrong conclusion earlier today that only a test writes `INSERT INTO markets`. Two attempts to trace it failed first (`$( )` strips NULs out of command output, and the shell mangled `$c:apps/...`), which is the reason to write the provenance down rather than leave it to the next person to re-derive.
> - **Built 2026-09-12, awaiting a collector deploy — S2, pair stability through the screener.** Migration `010_pair_stability.sql` redefines `screener_pairs` to project `pair_stability`, `long_stability`/`short_stability` and each leg's `stability_days`, and the `stability` sort key dropped during the sortable-columns work is back.
>   - **`least()` skips nulls instead of propagating them, which would have flattered exactly the wrong pairs.** Verified on the instance: `least(0.7, NULL)` returns **0.7**, not NULL — and **127 of 1,571 live assets have one scored leg and one unscored**. A bare `least(long, short)` would therefore have ranked those 127 by their *scored* leg alone, promoting the pairs whose data is incomplete, and `NULLS LAST` could not have helped because no null is ever produced. The function uses a `CASE` so an unscored leg poisons the pair figure, giving the ordering fragment a real null to push to the end. With only **5** assets wholly unscored, this would have hidden behind a small-looking null count.
>   - **The pair figure is the weaker leg, not an average.** A leg that keeps flipping hands back what the steady leg earns, so `least` is the honest summary; **764 of 891** pairable assets are fully scored, and the best weakest-leg in the book is **0.8780** — the shrinkage ceiling exactly.
>   - **The score is rendered as a bare ratio, never a percentage.** `formatApr` would print 0.854 as "+0.85%" and invite reading a persistence score as a funding rate, so a local `formatStability` gives two decimals with no sign or unit — the scale runs 0.5–0.878 and a reader expecting 1.00 at the top would misread every row. The day count rides along in the cell's `title`, because 0.69 over 6 charging days and 0.69 over 30 are not the same claim; a pair with an unscored leg says *which* leg rather than showing a bare dash that looks like a rendering fault.
>   - **Columns come free from a join that was already there.** `candidates` already `LEFT JOIN`s `market_funding_stats` for `apr_7d`, and stability lives in the same table, so this adds no join and no scan. OI stays omitted for the reason recorded above — it is per-leg only, and summing the winning pair's legs ranks by pair-selection artefact.
>   - **Dropped by the seven-argument signature, which is what is actually installed.** `pg_proc` shows only the 7-arg form in both `public` and `airates_it`, with no 6-arg overload surviving 005's own `DROP`; the worker calls it with six arguments and picks up `p_max_mark_deviation`'s default, so the installed arity is not what the call site implies. `migrate()` is a filename ledger with no checksums and wraps each file in a transaction, so the `DROP`/`CREATE` swap is atomic for the continuously-reading worker.
>   - *Proven against the real schema before production sees it:* 010 applied to `airates_it` at 14:22:48Z and `pg_get_function_result` confirms `pair_stability` and `long_stability_days` in its return type, while `public` still has the old definition. Typecheck and lint clean; **270 non-DB, 87 worker and 49 collector tests pass**. The fixtures pin the defect the migration exists for: an asset scored 0.87 on one leg and unscored on the other must sort *behind* an asset whose weaker leg is 0.60.
>   - **Not yet live, and the sequence matters.** `vaultdeck` is still on the old definition because the collector deploy (`./deploy/hklab/deploy.sh`, which is what runs `migrate()` in production) was declined by the permission classifier — restarting a service on a box shared with 16 tenants is the user's call. **The worker must not be pushed first:** its new `ORDER BY pair_stability` would hit a function without that column and break `/screener?sort=stability`. Collector deploy, then worker push.
>   - **Superseded plan text:** the entry below said S2 would order by `least(long_stability, short_stability)` directly. That is the bug this slice found; the minimum is computed inside the function instead.
>   - *Verified in production:* migration 010 applied to `public` at **16:07:40Z** (`migrations applied: 010_pair_stability.sql`), both schemas carry `pair_stability` and the per-leg day counts, and the fleet resumed normally (bybit 829, okx 479, mexc 1192, kucoin 682, gate 970, hyperliquid 178). The null-poisoning rule holds on live data: of 888 pairs, **854 carry a figure, 34 are nulled by a single missing leg, and 0 leaked** a lone scored leg. (That 34 is not the 127 measured earlier: 127 counted mixed-leg *assets* across all live legs, while `screener_pairs` picks only the cheapest and richest leg per asset, so the pairs actually chosen are a subset.)
>   - **The sort plateaus at the ceiling, and a day-count tie-break was considered and rejected as inert.** 132 of 853 pairs (**15.5%**) sit at exactly 0.8780, so the first page is eight-plus rows of identical scores. The instinct was to break ties on `stability_days` — but every ceiling pair has `min_days = max_days = 31`, because only a 31-day window can reach 36/41 at all, so that tie-break would sort nothing. Measured rather than assumed, and dropped. The existing `spread_apr` secondary key is already the meaningful question — among markets that never flipped for a month, which one pays most — so no change was made. Across the whole book the metric still discriminates: **92 distinct values** spanning 0.5000–0.8780.
>   - **Next — S2 projects the score through the screener.** `screener_pairs` must return `long_stability`/`short_stability` so the sort key dropped during the sortable-columns work can come back, ordered by `least(long_stability, short_stability)` — a pair is only as persistent as its weaker leg — with explicit `NULLS LAST`, since 85 markets carry no score. PostgreSQL cannot change a function's return type in place, so migration 010 starts from 005's exact text and drops the **seven**-argument signature first, exactly as 005 dropped the six-argument one. Both call sites (`data.ts:189`, `screener.int.test.ts:47`) use `SELECT *` and postgres.js keys rows by name, so added columns cannot break them positionally — checked before writing the migration rather than after. The comment at `data.ts:8-14`, which tells the reader stability "does not exist yet", is part of the same change.
> - **Shipped 2026-09-12 — fees the reader supplies (F1).** The plan recorded fees as blocked on the venue catalog, which has no fee fields. **That framing was wrong, and the user corrected it:** taker fees are per *account*, not per venue — CEX VIP levels key off 30-day volume, Hyperliquid tiers off 14-day volume, staking and referral discounts move them again — so any catalog figure would be wrong for most readers. The pair page asks for one instead, and the backtester finally reports a net figure rather than a gross one with a caveat.
>   - **The engine already did this work.** `BacktestFees`, `costsUsd` over four taker fills, `netAfterCostsUsd` and `paybackDays` were written and tested in Phase 3; `runBacktest` simply never passed `fees`, so `costsUsd` was permanently null. F1 is a wiring slice — `packages/core` is untouched.
>   - **Absent by default, never zero.** Null keeps the engine's "costs unknown" path; zero would assert that trading is free. An explicit 0 is honoured, since some venues rebate takers. One leg priced and the other blank charges **nothing at all**, because charging half a round trip understates it worse than reporting nothing. Malformed or negative reads as "not supplied".
>   - A number input rather than a menu: 1.5, 4.5 and 2.3 bps are all ordinary, and a menu would force a rounded lie. The page's standing claim that fees "aren't published consistently enough to assume one" is now conditional — it appears only when none were given.
> - **Shipped 2026-09-12 — stability and 30d trend on the asset page (F2).** S1 computed `momentum_30d` hourly for 4,701 markets and **nothing read it**; a stored column no page consumes is dead weight.
>   - **Measured before choosing where it goes, and the measurement killed two designs.** Of 796 assets with two or more scored legs, **501 have legs that disagree on direction** and only 295 agree — so a single momentum figure per asset would average two opposite stories for 63% of them, the same "number we would be inventing" that keeps open interest out of the screener sorts. And it cannot be a `/rates` timeframe: `HEATMAP_VALUE` maps every timeframe to an APR on one shared scale where red means "longs pay", so a market fading from +200% to +150% would render blue and invert that page's only invariant. The asset page is per market, which is momentum's actual grain.
>   - **Plain signed points with an arrow, not signed-log and not tinted.** The distribution is tight where it matters — 978 markets under 1 point, 2,217 between 1 and 10, only 217 beyond 100 — so compressing it would flatten exactly the near-zero distinctions a reader looks for. `aprTone` already means "who pays", so an arrow carries direction instead. Exactly zero reads "flat".
>   - `exchange()` selects the columns without rendering them: its table is already ten columns wide, but `MarketRow` now declares the fields and a query returning `undefined` for a promised field would typecheck while lying.
> - **Shipped 2026-09-12 — the homepage's verified 7-day ranking (F3), the last unbuilt item in the original Phase 3 text.** That text assigned it to "py-analytics' nightly run", a substrate that never existed — the same correction already recorded for `py-backfill`, VenueHistoryDO and the stability scores. Migration `011_pair_backtests.sql` adds `market_pair_backtests`; the collector replays it nightly.
>   - **Precomputed because the numbers demand it:** 652 candidate pairs against 281,930 settlements in 7 days. One query fetches all 1,304 legs in **74 ms** (measured, all buffers `shared hit`), but the replay is 652 engine calls — impossible inside a 10 ms request.
>   - **It reuses `backtestPair` rather than re-expressing the maths in SQL.** That engine sums each leg at its *own* settlement times and reports gaps as missed settlements rather than zero funding. A SQL twin would duplicate the one calculation this project treats as correctness-critical — and the verification section below used to demand parity between two implementations, a line deleted as moot once there was only one. The collector value-imports the engine; `@ai-rates/core` was already a workspace dependency.
>   - **The ranking is ungated, by the user's decision, so every row carries its own risk.** Ranking purely on what settled puts distressed listings first: IOST leads at **$835 per $10k** because its gate leg funds at **−962% APR on $0.68M** of open interest. Floors were measured first — $2M open interest, ≤200% APR, stability ≥0.6 would have yielded a defensible list (龙虾, CRO, UAI, BULLA, LUNC, NATGAS, every leg above $2M) — and **deliberately not applied**. Instead each row stores and shows the thinner leg's depth, the worse leg's absolute APR, and pair stability, so the danger sits beside the number. The candidate set mirrors the site's existing defaults ($250k OI, |APR| ≤ 1000, 5-minute freshness, 5% mark agreement); those are consistency with the screener, not new gates.
>   - **One floor, also the user's decision: both legs must have charged on all 7 days.** Without it a market with six charging days tops the table on a handful of settlements — STONK's legs have 6, 6 and 22. **4,004 of 4,838** markets clear 7/7 and **512 of 650** candidate pairs do, so it costs little. The day counts come from `market_funding_daily`, which already holds them, so the floor needs no second scan of `funding_events`.
>   - Rows are **dated per run**, not a single snapshot, so the page states its own freshness and a failed run leaves last night's ranking standing rather than a hole. At most 891 pairable assets exist, so this grows under a thousand small rows a night against `market_funding_daily`'s 344,598 rows at 84 MB; retention is 30 days, following that table's own rule. Each row links `?long=&short=` because `pairHref(asset)` alone opens whichever pair the asset page picks *by spread*, often not the pair that earned the row.
>   - *Three corrections of my own, all caught by measurement rather than review:* the replay first read **$36 instead of $42**, and the arithmetic was right — an earlier test in the same file had seeded three extra settlements on the shared symbol (long leg summed `21 × −0.0001 + 0.0006`), so the fix was its own asset, not a changed expectation. `market_pair_backtests` **leaked test rows** into the shared `airates_it` schema because its cleanup cannot join a `venue_id` loop (it keys legs as `long_venue_id`/`short_venue_id`). And an assertion of `+962.7%` was written from imagination — `formatApr` drops decimals past 100, so it renders `+963%`.
>   - *Verified:* migration 011 applied to `airates_it` (20 columns) and **deliberately not yet to `public`** — the collector deploy creates it there. Typecheck and lint clean; **279 non-DB, 96 worker, 50 collector** tests pass, including a `verifiedPairs()` integration test that runs the real `SELECT *` under `fetch_types: false` (the unit tests fake the DataSource, which is how a broken `text[]` once reached production).
>   - **Deploy order matters:** collector first, then push. The worker's `verifiedPairs()` would otherwise query a table that does not exist in `public`.
> - **Shipped 2026-09-13 — Turnstile on the uncached backtest.** The widget already existed on the Cloudflare account; its sitekey and secret live in `plans/access.md` (gitignored, and the secret was never read into a transcript or a commit).
>   - **"Turnstile when the result isn't cached" needs no logic at all.** `index.ts` answers from `caches.default` before `handleApp` runs, so anything reaching the gate is already a cache miss, and a solved challenge then populates the cache for an hour — the next caller of that URL pays nothing and sees no challenge.
>   - **The token travels in a HEADER, which corrects this plan's implied design.** `backtestForm` is `method="get"`, so Cloudflare's `cf-turnstile-response` form field would land in the query string. Three things break at once: the edge cache keys on the full URL, so every request would miss cache forever; a single-use, 300-second credential would be baked into links this site expects people to share; and it would put a credential in a URL. The API reads the header only, and a token supplied in the query string is deliberately ignored — pinned by a test.
>   - **`success` alone is not enough.** A token minted for another form, or solved on someone else's page, verifies as `success: true`. Verification also checks `action` (`backtest`) and `hostname` against an allowlist — that is what makes a token non-transferable. The allowlist is a constant rather than the request's own Host header, which a caller controls.
>   - **Fails closed.** A network fault, a non-200 or a body that is not JSON all refuse. This gate keeps an expensive `funding_events` read off a Postgres instance shared with 16 other tenants, and an open gate on error would be the same as no gate.
>   - **Placed after answerability, before the only expensive call.** Tokens are single-use and expire in 300 seconds, so challenging a request that was about to 400 or 404 would burn one on an error; a test asserts the verifier is never reached in those cases. And a 403 is returned `no-store`, because a cached rejection would lock out a legitimate caller for the whole TTL.
>   - **Sitekey in `wrangler.jsonc` vars; secret as a Worker secret.** The sitekey is public by design (it ships in the widget markup), so `wrangler types` picks it up. The secret is deliberately *not* declared as a var: a var of that name would overwrite the real secret with the config value on the next deploy. It is read through one narrow, documented cast in `index.ts` — because ambient `interface Env` merging does not work here, which was measured rather than assumed: a probe `.d.ts` under `apps/worker/src` failed to merge, since `tsconfig.json` pins `"types": ["./worker-configuration.d.ts"]`.
>   - **That hole is now closed (2026-09-13).** The entry previously recorded it as open: the gate covered `/v1/pairs/:asset/backtest` only, so asking for `/pair/:asset?long=…&short=…` ran the same replay ungated. The page now uses the clearance flow this entry sketched — widget, a POST to `/pair/:asset/verify`, a short-lived signed cookie, then a 303 back to the shareable GET — and `TURNSTILE_SITEKEY` is read by that page.
>     - **The cookie is an HMAC over its own expiry**, keyed on the Turnstile secret. A stateless check means the expiry has to travel in the cookie, which makes it attacker-visible, so it must be authenticated or clearance could be extended by editing one number. A test pins exactly that, alongside a forged-secret case and a truncated-signature case.
>     - **The challenge page is a 403, not a 200.** `index.ts` caches only 200s by URL, so a 200 would let the challenge be stored *in place of the result* and served to everyone — including readers who already hold clearance. `page()` stamps `no-store` on non-200 as the second guard. This was the trap that shaped the whole design.
>     - **No open redirect:** the destination is rebuilt from the posted fields through `backtestToQuery`, never taken from the request, which also lands on the same cache key a shared link produces.
>     - **Only work is gated.** Browsing `/pair/:asset` to choose two legs is free; the limiter and the challenge apply solely to a request that would actually replay. And once one reader solves it, the cached result serves everyone else for that combination.
>   - **The rate-limit binding landed with it**: 20 requests per 60s, keyed on the connecting IP in separate buckets for the page and the API, called **before the first database read** — a test asserts no query ran when the limit refused. Per-colo, so it is a first filter and not a bound.
>   - **Production is ungated until the secret is set.** With no `TURNSTILE_SECRET` the gate is skipped, which is what lets `wrangler dev` and the test suite run at all; `.dev.vars.example` documents that. `wrangler secret put TURNSTILE_SECRET` is the remaining step and writes to the live Worker.
>   - *Verified:* 10 verifier unit tests, 48 assertions, with an injected `fetch` — no network and no global mock, following the adapters' own `FetchLike` dependency injection rather than monkey-patching, for which this repo had no precedent. Typecheck and lint clean; **292 non-DB and 109 worker** tests pass.
> - **2026-09-14 — the pair page's remaining ORBIT items: each leg's window average, the result's range, and a price-gap link.**
>   - **Each leg reads "now X, 7d avg Y, every 8h".** `LegResult.averageAprPercent` is the market's rate over the window, time-weighted as sum(rate) / sum(basis hours) like the rollups, in the market's own sign regardless of the side held; hours with no recorded funding neither lower nor raise it. Computed in both engines, so the page and `/v1/pairs/:asset/backtest` agree.
>   - **Best day, worst day and max drawdown** join the facts line. They come from the shared `summarize()`, so the replay and the daily engine cannot drift; drawdown is the largest fall in cumulative funding from a previous high, measured from zero and before costs, the same basis as the curve.
>   - **A "price gap" link to `/price-pair/:asset`** in the form's actions, for entry and exit at the books, rather than duplicating that page.
>   - *Verified:* 196 core and worker tests, including the two engines agreeing on all three and a hand-worked drawdown; 38 collector unit tests; typecheck across worker, collector and tests. Checked in Chrome against the local preview: the legs and the facts line render on one row each.
> - **2026-09-13 — the pair page compares every exchange's funding (`web/funding-chart.ts`).** Taken from ORBIT's pair page, which draws its two legs and a spread, and extended with the one thing it lacks: every exchange listing the asset is on the chart, the legs drawn and the rest a checkbox away. With no legs chosen, all of them draw.
>   - **Step lines, not slopes:** funding is fixed between settlements, so a line holds each rate until that venue's next settlement; ORBIT's diagonals imply rates nobody paid. A day without an hourly row (three days on the daily grain) breaks the line rather than drawing across it. The spread holds each leg's last rate, never interpolates.
>   - **Signed log scale**, the rails' own, so a distressed −2,500% and an ordinary +10% stay readable together. The scale spans every market, visible or not, so a toggle never rescales what is being read.
>   - **Server-drawn SVG plus a small script** for toggles and a hover readout of every visible line at one moment; no chart library, as the equity curve. The payload travels as [bucket offset, APR], 3.5 kB for six venues over 7 days.
>   - **Grain by window:** 1/3/7 days read `market_funding_hourly`, 15/30/60 read `market_funding_daily`. The chart read is fail-soft: until the collector applies 014 it says so, and the backtest beside it still renders.
>   - **Also:** a 1d–60d window strip in place of the days select (it keeps legs, size and fees), a swap-legs link, and each leg's rate now and cadence beside what it settled.
>   - *Verified:* 187 unit and route tests, 20 worker DB integration tests including `hourlyFunding()`. Checked in Chrome against a local preview of the real `handleApp`: toggling a venue draws it, the readout reports every visible line at the cursor, 30 days switches to the daily grain, and no console errors.
> - **2026-09-13 — an hourly funding rollup for the pair page's short-window chart (migration 014).** `market_funding_hourly` holds one row per market per UTC hour, as sums like the daily rollup, and keeps 8 days (about a million rows). The chart's 1D/3D/7D windows read it; 15D and longer read `market_funding_daily`.
>   - **Why hourly and not the 4-hour buckets first proposed:** 4 hours gives a 1-day chart six points per venue, and retaining 70 days of it would need a 70-day rescan every hour, doubling the daily fold's load on a database shared with other tenants. Hourly for 8 days rescans a small fraction of that, and an 8-hourly market's settlement landing in one hour and holding until the next is exactly how funding accrues.
>   - **`refreshHourlyFunding` rescans the whole retained window each run**, like `refreshDailyFunding`, so a late backfill still lands in its hour. The window starts on a whole hour, since a partial first hour would overwrite that hour's full sum, and truncation is explicitly UTC. It runs inside the existing hourly long-windows task.
>   - **Deploy order:** the collector applies 014 on start, so it must ship before a worker that reads the table; the chart that reads it fails soft regardless.
>   - *Verified:* 8 collector store integration tests including the new fold (two settlements in one hour sum; a 9-day-old event stays out), and 198 collector and worker tests together. A first combined run showed 3 worker DB failures that did not reproduce, neither on this tree nor on the commit before it, and the worker DB suite alone passes 19/19: shared `airates_it` interference, not this change.
> - **2026-09-13 — the backtest reads the daily rollup, and the Turnstile gate above is gone.** The gate protected a `funding_events` replay. `/pair/:asset` and `/v1/pairs/:asset/backtest` now read `market_funding_daily` (two markets × at most 60 rows) through `backtestDaily`, which returns the same `BacktestResult` shape, so the page and the JSON contract are unchanged. Funding is linear in size and fees, and the replay already bucketed by UTC day, so any size or fee a reader types still works without precomputing it.
>   - **What changed in meaning:** a window is whole UTC calendar days ending today (7 days at noon is 6.5 days elapsed), not a rolling 168 hours. Missed settlements come from each day's covered hours, `24 − basis_hours_sum`, counted only between a leg's first and last recorded day and never on the day still running. A core test holds both engines to the same funding, per-day, win-rate and cost figures over whole days, and to the same missed count for a short day.
>   - **Removed:** `turnstile.ts`, `clearance.ts`, the challenge page, `POST /pair/:asset/verify` and the sitekey var. The per-colo rate limiter stays. `TURNSTILE_SECRET` is still set on the Worker; `wrangler secret delete TURNSTILE_SECRET` clears it.
>   - **Windows are 1/3/7/15/30/60 days**, capped at 60 by the rollup's 70-day retention (the replay allowed 90).
>   - **No collector deploy needed:** `market_funding_daily` has existed since migration 008.
>   - *Verified:* 220 core and worker tests, and 19 database integration tests against `airates_it` including the new `dailyFunding()` read. Typecheck and lint clean.
> - **2026-09-13 — the public repo was leaking the deploy target; scrubbed, and main's history rewritten.** `raw.githubusercontent.com` served `deploy/hklab/deploy.sh` unauthenticated (`"private": false`), so the SSH user, hostname and port were readable by anyone — and the runbook added the internet-facing Postgres port, the database role and the `DATABASE_URL` shape.
>   - **`deploy.sh` now sources `deploy/hklab/.env.deploy`** (gitignored) and **refuses to run** when any value is missing, rather than falling back to a hardcoded default — a stale target is a worse failure than no deploy. `.env.deploy.example` is the committed template, which needed a `.gitignore` negation because `.env*` would otherwise swallow it, exactly as `.dev.vars.example` did.
>   - **Scrubbed from four files, not one.** Fixing only `deploy.sh` would have implied the problem was solved while the same host stayed readable in `README.md`, `wrangler.jsonc` and this plan.
>   - **History rewritten with `git filter-repo` over `refs/heads/main` only:** 57 commits, **0 surviving blob hits and 0 surviving message hits**. The SSH port needed *context-anchored* rules rather than a bare replacement, because `py/spike/uv.lock` holds a package hash containing those digits (`64d826341d61c`). The arithmetic reconciles exactly: 149 `-p` + 53 `:port` + 53 `SSHPORT=` = 255 in our files, plus 55 in the lockfile = the 310 bare hits. A naive global replace would have corrupted that lockfile in every commit.
>   - **Every SHA on main changed** (tip `7b7086d` → `06024ba`), so the commit hashes cited in the entries above — `4cfb37e`, `1dcb5d5`, `cd31ac7`, `ea4262a`, `67a69a2`, `5a145aa` and the rest — **no longer resolve**. They are kept as written because they still describe the order the work happened in.
>   - **`refs/heads/display-airrates` was deliberately NOT rewritten.** It backs another session's live worktree and exists only locally, so rewriting it would have yanked a checkout out from under running work while fixing nothing public. **It still carries the values and must be rebased onto the rewritten main before it is ever pushed.**
>   - **Credential audit came back clean**, which was the thing worth checking first: `plans/access.md`, `.env`, `.env.test.local`, `.dev.vars` and `deploy/hklab/.env` have **0 commits each**, and no historical blob contains a password assignment. The database password was never published. The committed CA certificate carries no hostname in its subject or SANs.
>   - **Still outstanding, and none of it is cosmetic.** (1) A **force-push** is required to publish the rewrite. (2) The GitHub PAT embedded in the remote URL was found **live** — `api.github.com/user` answered 200, correcting an earlier note in this session that called it dead — with scopes `repo, user, workflow, write:packages` expiring 2026-10-09; it needs revoking, and the remote re-adding without an embedded credential. (3) The Postgres port is still internet-facing with `pg_hba.conf` as `host all all all`, so **TLS is enabled but not enforced**. (4) A rewrite recalls nothing already cloned, forked, mirrored or indexed, and GitHub keeps unreachable objects addressable by SHA for a period afterwards.
> - **Note:** the original plan's Phase 3 text below predates the hklab architecture. There is no `py-backfill` Container, no VenueHistoryDO and no R2: history lives in `funding_events` in vaultdeck, and the backfill is part of the collector.
- `py-backfill` Container pulls the maximum funding-history lookback + klines (for `mark_px`) across all markets on the 10 venues. Idempotent upserts into VenueHistoryDO + R2. Progress tracked in D1.
- `packages/core/math/backtest.ts` + `/v1/pairs/:sym/backtest` (cache key: sym, long, short, size, days, UTC hour; 1h TTL; Turnstile when the result isn't cached) + `/pair/$sym` UI with equity curve.
- ~~`py-analytics` hourly job computes stability, 7d/30d settled averages and momentum. Its nightly run produces the homepage's "verified 7-day backtests" top 10.~~ **Done 2026-09-12, without `py-analytics`:** the settled windows are migration 008, stability and momentum are 009/010, and the nightly verified top 10 is 011 — all computed by the collector in SQL and TypeScript. See the F1–F3 entries above.

### Phase 4 — Price arbitrage (week 8)
> **Added 2026-09-12: do liquidations predict funding regime switches?** Study whether liquidation activity has a regression relationship to switches in funding-rate patterns, then visualise it for the top assets.
>
> - **The honest starting point: we have none of the required data.** A repo-wide search for "liquidat" returns nothing, no adapter exposes a liquidation or kline endpoint, and the database has 8 tables — funding, markets, venues, runs. The study cannot begin until liquidation ingestion exists, so that is the first deliverable, not the analysis.
> - **Prefer liquidation *events* over a liquidation *heatmap*.** A Hyblock/Coinglass-style heatmap is a **model** — estimated liquidation levels inferred from open interest, assumed leverage tiers and price — not observed data. Forced closes are directly observable on venue websockets (Binance `forceOrder`, Bybit `liquidation`, OKX, Hyperliquid). For a regression, observed events are the sounder regressor; a modelled heatmap would mostly measure our own leverage assumptions. Build the event feed first; a heatmap can be derived later if it earns its place.
> - **Data source is an open decision.** Self-collect via websockets on hklab (no licence cost, new long-lived WS ingestion alongside the REST collector, and the only route that keeps us clear of redistribution terms), or buy a third-party feed (faster, but paid and squarely in the exchange-ToS risk already flagged in `plans/phase0-referrals-legal.md`). Recommend self-collecting the top venues.
> - **The funding side is already rich:** 387,565 sign flips across 3,464,934 settlements in 90 days, so "regime switch" has ample material. Define it precisely before fitting anything — a sustained sign flip, or a z-score break against the market's own trailing distribution — and pre-register the definition, or the study will find whatever it is asked to find.
>   - **Measured again 2026-09-13, before any modelling:** 3,491,275 settlements across 5,337 markets over 91 days (2026-06-13 to 2026-09-12). Over the last 30 days, **87,732 sign flips in 1,088,309 charging settlements — a 8.06% base rate**. That figure is written down *now*, unprompted by any result, because it is the number any predictive claim has to beat: a model that calls flips 8% of the time and is right 8% of the time has learned nothing, and without a pre-registered base rate such a model is easy to present as a success.
>   - **A probe that contradicts this plan's own assumption.** The text above says liquidations are observable "on venue websockets" and treats WS ingestion as the cost of self-collecting. **OKX publishes a *public REST* endpoint** — `/api/v5/public/liquidation-orders?instType=SWAP&state=filled` returned 200 with `bkPx`, `posSide`, `side`, `sz` and `bkLoss` per record — which the collector's existing HTTP client, rate limiter and circuit breaker could poll on a `PeriodicTask` with **no new long-lived infrastructure at all**. Meanwhile Binance's `allForceOrders` is **404/gone**, so the endpoint this plan implicitly assumed is not available. The "self-collect vs buy a feed" decision should therefore be re-costed: a REST-first slice on the venues that serve it is far cheaper than the websocket build the plan priced.
> - **The price series is young, not broken.** `funding_snapshots` holds 2 days (4.58M rows, ~860 samples/24h for bybit BTCUSDT, about one per 100s) against a 30-day retention policy, and it is accumulating normally — `collector_runs` starts on the same date, so live collection genuinely began 2026-09-11 and nothing was dropped. The earlier `funding_events` history reaches 2026-06-13 only because it was *fetched* from venue history APIs and backfilled, which snapshots cannot be. So an event study must either wait for the series to fill toward 30 days, or collect klines. Do not read the short span as data loss.
>   - **Re-measured 2026-09-13, and the note above is right for a reason it did not give.** `funding_snapshots` now holds **8,091,482 rows** spanning **1.05 days** (2026-09-11 → 09-12), bybit BTCUSDT sampled every **60.2s**, and `mark_price` present on **every** row. A shorter span than the "2 days" recorded on 09-12 looks like loss but is not: hourly coverage is a smooth ~324,000 rows/hour with **no gap**, and the newest snapshot is **0.0 minutes** stale. The table carries *two* policies — retention `drop_after: 30 days` **and compression `compress_after: 1 day`** — so only the last day is uncompressed; nothing is being dropped. The event study still has to wait for the series to fill, and `mark_price` coverage means klines are not needed for a price regressor.
>   - **Production is behind local main by exactly one table.** `schema_migrations` tops out at `010_pair_stability.sql` and `market_pair_backtests` does **not** exist, because the collector deploy carrying 011 never ran; `screener_pairs` *is* the S2 version (`pair_stability` present). The homepage fail-soft added on 09-13 is what keeps that from 503ing the page, which is the case it was built for.
>   - *Debt found the same day:* the collector **exited** with `PostgresError: Connection closed (ERR_POSTGRES_CONNECTION_CLOSED)` mid-backfill when the database restarted, then came back on the old image. The fleet self-healed (`ok:true`, bybit 829, okx 479, 0 errors) and nothing was lost, but a dropped connection should be retried rather than fatal.
> - **Shipped 2026-09-13 — L0 and L1: forced closes are being ingested.** Migration `012_liquidations.sql`, a `fetchLiquidations?` adapter hook, a Gate implementation, a 5-minute collector task and an insert-only store writer. **Only Gate has the hook**, so exactly one task exists.
>   - **`size`, not `order_size`, gives the side — and measuring that is what stopped a backwards regressor shipping.** The two fields are opposite in sign in **167 of 167** live records: `size:"76", order_size:"-76"` is a liquidated LONG closed by a sell. I had planned to key on `order_size`, which would have inverted every long and short in the study and produced confident, exactly-wrong conclusions. The sign is consumed into `side`; the stored size is absolute.
>   - **Units measured as in B2:** size is CONTRACTS, so notional is `size × quanto_multiplier × fill_price`. Both the raw size and the derived notional are stored so a conversion error stays recoverable, and a contract with no known multiplier gets a **null** notional rather than a guess.
>   - **De-duplication is the load-bearing assumption, and it is proven end to end.** Gate accepts `from`/`to` and **silently ignores them** — shown with a bogus-parameter control that returned the identical first record — so there is no resumable window and every poll re-reads the page. A real sweep against the test schema stored **169 of 169** on the first pass and **0 of 169** on the second. The documented cost: two liquidations identical in all five key fields within one second collapse to one, which biases counts **down** in the busiest moments.
>   - **Cadence 5 minutes, 12× margin:** the deepest page covers ~58 minutes at ~3 records/min, for 24 requests an hour including the contracts call.
>   - **L0 had to come first.** The collector *exited* on `ERR_POSTGRES_CONNECTION_CLOSED` when the database restarted, because the pooled client surfaces connection faults asynchronously where no call-site `try` can reach them. A narrow top-level handler logs and continues for connection faults only; a `TypeError` still crashes loudly, which a test of the classifier confirms.
>   - *Verified:* 311 non-DB, 124 worker, 52 collector tests pass; migration 012 applied to **`airates_it` only**. **Production is still on `010_pair_stability.sql` with no `liquidations` table**, so it needs a collector deploy for 011 *and* 012 before any of this runs live.
>   - **Two corrections to my own claims, both from this session.** (1) I reported the `store.ts` NUL byte as resolved by `4cfb37e`; a byte census found four more — but they are a **deliberate idiom**, `` `${venue_id}\0${venue_symbol}` `` as a composite map key in four places, chosen because a NUL cannot occur in a symbol. I nearly "fixed" working code on the strength of a census. `recordLiquidations` now uses the same separator instead of spaces, for consistency. (2) **Plain `grep -P` on macOS silently matches nothing** rather than erroring, so three of my control-character checks reported a clean file that demonstrably had four. Use `git grep -P` or Python for this, never plain `grep -P`.
>   - **L3 cannot be fitted yet, and the arithmetic says why — this is a time constraint, not an effort one.** Gate yields ~3.0 records/min ≈ **4,300 liquidations a day** across ~50 contracts, i.e. **~86 events per contract per day**: thin for daily buckets, workable at weekly ones. Liquidation history starts from **zero today**, while the funding side already has 91 days and a pre-registered 8.06% flip base rate. So roughly 7 days gives ~30k rows, 14 days ~60k, 30 days ~130k — and a panel regression with market fixed effects needs enough events *per market*, not in total. Fitting anything before that accumulates would be the exact failure this plan warns about: a study that finds whatever it is asked to find. **The next Phase 4 action is to let the ingestion run.**
>   - **L3 ran on 2026-09-13 and returned a null on both sides.** Specification pre-registered and committed as `e075a5e` *before* the estimator ever saw real data; `plans/phase4-l3-preregistration.md` and `py/l3/`. Run once, nothing uncommitted, 3,749 events across 175 markets.
>
>     | side | events | markets | DiD | 95% CI | leave-1-out | pre | zero% |
>     | --- | --- | --- | --- | --- | --- | --- | --- |
>     | long | 2,398 | 174 | +6.3290 | [−6.3714, +18.7149] | +6.3385 | −6.5290 | 29.1% |
>     | short | 1,351 | 143 | −1.7469 | [−14.1940, +9.6909] | −1.8250 | −9.9423 | 38.0% |
>
>     - **Both intervals span zero, and this is a well-powered null, not a shrug.** 174 and 143 markets against a pre-registered floor of 10; leave-one-market-out barely moves either estimate (+6.3290 → +6.3385, −1.7469 → −1.8250), so the market holding 29% of events drives nothing. Neither `UNDERPOWERED` nor `FRAGILE` fired.
>     - **The point estimates lean opposite to H1** — long positive, short negative, where the hypothesis predicted long cascades pushing predicted funding *down*. The intervals are far too wide to make anything of that, and it is recorded only so it cannot later be reported as directional support.
>     - **The pre-trend diagnostic fires loudly.** `pre` is strongly negative on **both** sides (−6.53, −9.94): predicted funding was already falling before these events. That is the confound the plan warned about from the start — both series tracking price — and it is the most informative thing the study produced.
>     - **The bullet above was right for the wrong reason.** "Let the ingestion run" was correct for the plan's *settlement-resolution* design, where 8-hourly venues give ~4 settlements per market. But the binding constraint was the **specification**, not the data: `funding_snapshots` samples every market every 60s and stores the venue's drifting *predicted* rate, so a minute-resolution event study was feasible immediately. Waiting would not have fixed a design problem.
>     - **What this does not license.** It is 1.5 days, one regime, two venues, *predicted* rather than settled funding, and a single 30-minute horizon. It is no evidence that liquidations do not matter; an effect at a longer horizon, or at settlement resolution once months exist, is entirely consistent with this.
>   - **L4 is not applicable at this sample.** It was specified as visualising the L3 relationship, and there is no relationship to visualise — charting a null would dress up noise. Revisit with L3 once the series reaches the 30+ days the original settlement-resolution design needs. The one visualisation that *would* earn its place now is an event-time plot of `Δapr` at −30/−20/−10/0/+10/+20/+30, because the pre-trend is more interesting than the effect.
>   - **Track B (price arbitrage) is the phase's actual title and is untouched.** A repo-wide search for `arbitrage`, `price-pair`, `book-ticker` and `MarketHub` returns **nothing**, and no adapter exposes orderbook or ticker depth. Its assumed substrate does not exist either: `wrangler.jsonc` has `durable_objects`, `hyperdrive` and `ratelimits`, but **no `pipelines` and no `r2_buckets`**, and `MarketHubDO` is not a class — only `ProbeDO` is. It also wants 5–15s book-ticker polling against a collector that runs at 60s with a 10s floor. That is the same "substrate that never existed" correction already applied to `py-backfill`, VenueHistoryDO and `py-analytics`, and it should be re-costed against the hklab architecture before any of it is built.
> - **Method sketch:** per market, per interval — liquidation notional and count (long vs short) as regressors; outcome is the funding-rate change or the probability of a sign flip in the next N settlements. Panel regression with market fixed effects, clustered by venue. Report effect sizes and confidence intervals, and state plainly that this is association, not causation — funding and liquidations are both driven by price moves, so any naive fit will be confounded.
> - **Visualisation, top assets:** a per-asset panel pairing the funding series with liquidation intensity on a shared time axis, plus a scatter of liquidation notional against subsequent funding change. Same constraints as the rest of the site — server-rendered SVG, no chart library, 10 ms CPU — so it reuses the sparkline/equity-curve approach already built in `pages.ts`.
>
- ~~Book-ticker polling every 5–15s for pairable assets on tier-1 venues, served only from MarketHubDO (not persisted at that resolution; 1-min to Pipelines). `/arbitrage` + `/price-pair/$sym` with spread history from R2 SQL rollups.~~ **Re-costed 2026-09-13, and it is far cheaper than this line assumes.**
  - **We already receive top-of-book, every cycle, and throw it away.** The three venues that publish it — gate (`highest_bid`/`lowest_ask`), okx (`bidPx`/`askPx`), bybit (`bid1Price`/`ask1Price`) — are exactly the three whose ticker endpoints the adapters *already call each minute* (`${BASE_URL}/tickers`, `/api/v5/market/tickers?instType=SWAP`, `/v5/market/tickers?category=linear`). **Size comes with price on all three** (`highest_size`/`lowest_size`, `bidSz`/`askSz`, `bid1Size`/`ask1Size`).
  - **So the cost is a few nullable fields, not an architecture.** No `MarketHubDO`, no Pipelines, no R2 SQL rollups, and **one new bulk request in total** (okx `/public/instruments`, see the build record below; gate and bybit genuinely cost nothing) — against a plan that priced 5–15s polling plus three bindings that do not exist (`wrangler.jsonc` has `durable_objects` with only `ProbeDO`, `hyperdrive` and `ratelimits`; the collector's interval floor is 10s). Same correction already applied to `py-backfill`, VenueHistoryDO and `py-analytics`: the substrate was never built, and the data turned out to be in hand.
  - **What it honestly supports, and what it does not.** Level-1 depth gives a *quotable* spread at the size shown. It says nothing about the book below level 1 or slippage at real size, so an `/arbitrage` page must say "best bid/ask, at the size quoted" rather than implying a fillable trade — the same discipline that stopped the pair page quoting headline leverage as if it held at any size.
  - **MEXC publishes it but not for free.** `bid1`/`ask1` live on `/api/v1/contract/ticker`, and our adapter calls `/api/v1/contract` (detail), which does not carry them — so MEXC costs one extra request per cycle, unlike gate/okx/bybit where it is genuinely zero. kucoin shows none on `/contracts/active`; hyperliquid and dydx were probed at paths we do not use, so those two are inconclusive rather than negative.
  - **A naive symbol join would ship spectacular fictional spreads, and measuring it proved that.** Joining OKX to Gate by asset produced top "gaps" of **13,660,780 bps** (KR200), 9.6M (BB) and 5.3M (ON). Those are the mismatched-instrument class migration 005 already fixed for funding: KR200 quotes 1096.67 on OKX against 0.7973 on Gate, a **1375.5×** disagreement — and 005's own header records "KR200 disagrees 1371x", so this independently re-derives a Phase 2 finding. ANTHROPIC and OPENAI sit at ~10×, a share-vs-fraction scaling difference rather than a name collision; BTC and ETH agree at 1.0× as the control.
  - **With 005's 5% mark-agreement rule applied**, 7 assets drop out and the picture becomes honest: 432 comparable assets, 177 with a positive gap, **median 0.0 bps**, top 269.6 bps (ONE). So `/arbitrage` is worth building but is mostly zeros with a thin tail, and any headline figure needs the same scepticism the verified-backtest ranking carries — a wide quoted gap on a shallow book is not a trade. **Any price view must pass through `screener_pairs`' existing `p_max_mark_deviation` guard rather than a raw symbol join.**
  - **The widest surviving gap is a quote artefact, and its numbers say so plainly.** `ONE` showed 269.6 bps buying OKX and selling Gate — but OKX's **ask size is 2 units** against Gate's 220,004, with 24h volume of 494M on one side and 58k on the other. There is no trade there, only a thin top-of-book on an illiquid quote. Any `/arbitrage` row must therefore carry its **level-1 sizes** beside the spread, exactly as the verified ranking carries thinner-leg open interest: a gap without the size next to it is a number that invites a loss.
  - **Slice shape, if built:** two nullable fields on `FundingSnapshot` after `indexPrice`, two columns on `market_latest` (migration 013), a two-line parse change in each of `gate.ts:45`, `okx.ts:52`, `bybit.ts:26` where the ticker types already live, and a page. No new requests for those three venues. **Sizes must be stored alongside the prices** — storing the spread without the depth would bake in the very omission the `ONE` case exposes.
  - **Built 2026-09-13 (data layer only; the page still waits on `pages.ts`).** `bestBid`/`bestBidSizeUsd`/`bestAsk`/`bestAskSizeUsd` on `FundingSnapshot`, migration `013_top_of_book.sql` on `market_latest`, parses in all three adapters, written from `latestRow` only. 377 tests pass, typecheck and Biome clean.
    - **The estimate above was wrong twice, and both corrections are the interesting part.**
    - **(1) The three venues do not agree on what a "size" is, and the raw numbers are a 10,000× trap.** For one BTC book the fixtures give gate `2776`, okx `504.48`, bybit `0.181` — depths of roughly **$21.6k, $392k and $14k**. gate quotes contracts (× `quanto_multiplier`), okx quotes contracts against `ctVal`, bybit quotes **base coin** (proved from its own fixture: `openInterestValue / openInterest` = 77,766.7, exactly the mark). Printing those side by side on the one page whose purpose is showing a book is too thin would be the same class of error as the inverse-contract 77,742× bug and KR200's 1375×. **So the stored column is USD notional**, converted in each adapter where the multiplier is already in hand — matching `open_interest_usd` and `liquidations.notional_usd`. The `ONE` evidence at 269.6 bps was itself quoted in raw units ("ask size is 2 units against 220,004"), i.e. the trap was already latent in the note that motivated the feature.
    - **(2) okx costs one extra bulk request after all, not zero.** `parseOkxSnapshots` never received instruments, and `ctVal` is the only thing that says what a contract is worth — so `fetchSnapshots` now also calls `/public/instruments?instType=SWAP` (**479 rows, one call per cycle, not per market**) and the parse gained a parameter. **15 swaps are `ctValCcy: "USD"`** (inverse), which the existing `contractNotionalUsd` already handles correctly. The adapter test now asserts **five** endpoints rather than four, deliberately, so any further per-cycle request has to be argued for there.
    - Fields are **optional** (`?:`), following `maxLeverage`'s documented precedent: only 3 of 10 venues publish top of book, and a required null would touch every adapter and fixture for a field they cannot fill. Prices go through `perUnitPrice` like mark and index; USD depths are money and are never rescaled.
    - **`/arbitrage` shipped 2026-09-13, and the live data says the depth column was the point.** `arbitrage()` in `data.ts` (the `screener_pairs` CTE shape reused, not a second guard), `parseArbitrageParams`, the page, a `/v1/arbitrage` endpoint, a fifth nav entry and hotkey `a`. 390 tests pass, typecheck and Biome clean.
      - **Measured against production the moment it worked, and it corrected this plan's own figures.** The note above says "432 comparable assets, 177 with a positive gap, median 0.0 bps" — that was a raw symbol join before bybit carried quotes. The guarded query now reports **721 comparable assets, 395 with a positive gap, median 1.58 bps, max 322.7**, and **zero rows with an unknown depth**. I pasted the stale figures into the page's own lede first, one turn after writing "any copy carried over from that note needs re-deriving rather than pasting" — caught only by measuring.
      - **The gap and the book are inversely related, across the whole table rather than in one anecdote.** The leaders: STORJ 317.2 bps good for **$568**, ONE 296.6 bps for **$18**, LSK 149.1 for **$10**, YOFC 105.7 for **$3**, ARK 94.9 for **$3**. The `ONE` case that motivated storing sizes was not an outlier; it is the shape of the entire top of the ranking. Rows stay ranked by gap — that is the question the page answers — with the size beside it, exactly as the ungated verified ranking carries thinner-leg open interest.
      - **Two self-inflicted bugs worth recording.** (1) `Number("")` is **0, not NaN**, so an absent `min_bps` parsed as an explicit zero floor and the default never applied; `parseTakerBps` in the same file already guards this and the new parser now does too. (2) `formatBps` already existed in `pages.ts` as a *prose* trimmer ("5 bps long"); the new column formatter needed the opposite rounding ("0.0"), so it is `formatGapBps` in `format.ts` and the two coexist deliberately.
      - *Environment note:* hklab runs **two** Postgres containers — `timescaledb_container` publishes `5437->5432` (ours) and a separate `postgres_container` publishes `5432->5432`. I rebuilt the test tunnel against 5432 and authenticated our credentials against the wrong tenant's database until it failed closed with `28P01`. The correct forward is written in `.env.test.local`'s own header: `-L 55437:127.0.0.1:5437`.
    - **`/price-pair/$sym` shipped 2026-09-13, and it deliberately inverts the list page's rule.** `priceQuotes(base)` in `data.ts`, the page, a `/v1/price-pair/:asset` twin, and the `/arbitrage` asset column now links here rather than to the funding page. 395 tests pass.
      - **The list page must hide a mismatched instrument; the detail page must show it.** `/arbitrage` drops any venue whose mark sits more than 5% from the asset's median, because otherwise a 1375× disagreement publishes a 13,660,780 bps "opportunity". But a reader who can see that exchange listed elsewhere is owed an answer, so `priceQuotes` returns every venue with `mark_agrees` and the `median_mark` beside it, and the page renders the rejects dimmed with how far out they are — "marked 1375.0× above the median for this asset, so it is a different instrument, not a price gap". Same guard, opposite presentation, because "why is that exchange missing" is the question a detail page exists to answer.
      - The cross-venue gap is computed **only among agreeing venues**, so a mismatch can never set the price even though it is on screen. The integration test asserts exactly that: three rows returned, the 1375× one flagged `false`, and the gap still the honest 50 bps.
      - Each row also carries the venue's **own** bid-ask spread, which a taker crosses on entry and again on exit — the cross-venue gap is not the only cost, and showing one without the other would flatter it.
    - **Net-of-fees is designed and defined, and deliberately not yet displayed (2026-09-13).** `packages/core/src/fees.ts` holds the one definition: `VenueFees`, a venue-keyed `FeeSchedule`, `gapCost()` and `backtestFeesFrom()`. Nothing renders it until real fees exist, because this project does not assume a fee.
      - **Two fills, not four — and getting this wrong would have understated every row.** `backtestPair` charges `size × (long + short) × 2` because a funding carry opens *and closes* both legs. A price gap is captured once: buy on one venue, sell on the other. And `gap_bps` is already computed bid-to-ask, so both venues' own spreads are inside it and must not be charged again. Reusing the carry's cost model would roughly double the cost. Different trade, different cost function — hence a separate explicit function rather than an overloaded one.
      - **Why this waits on member settings rather than a query parameter.** `/price-pair` shows 2–3 venues, so per-venue fees would fit in a URL. **`/arbitrage` ranks many different venue pairs at once**, so it needs a fee schedule keyed by venue, not a pair of numbers — which is member state, not a link. A shareable URL carrying someone's VIP tier is also the wrong artefact.
      - **Taker is unconditional, transfer is not.** A missing taker fee makes `netBps` null outright. A missing withdrawal cost leaves `netBps` computed *without* it and reports `transferBps: null`, because a transfer is paid only to rebalance. Any view showing `netBps` with a null `transferBps` **must say the transfer is not counted** — that asymmetry is exactly where a flattering number would otherwise sneak in. The withdrawal charged is the *buy* venue's, since that is where the asset sits.
      - `takerBps: 0` is a real answer (some venues rebate takers) and is distinguished from absence by `Number.isFinite`, not by truthiness.
      - **Across the language boundary:** the Go executor in `profitlock-worker` must not re-derive this policy. The arithmetic is trivial; the *policy* is not, so `fees.ts` is the authority and both sides are pinned to shared test vectors rather than shared code.
    - **Process note: never edit a migration after it has run.** The first version of 013 was applied to `airates_it` by a test run, then renamed (`best_bid_size` → `best_bid_size_usd`) while keeping the same filename — so `migrate()` saw `013_top_of_book.sql` in `schema_migrations` and skipped it for good, and 9 integration tests failed on a column that did not exist. Repaired by dropping the four empty columns and deleting that one `schema_migrations` row so the corrected file re-applied. `public` was never touched; production gets 013 from `main.ts:47` on deploy.
- **Public MVP launch at weeks 8–10 with ~15–20 venues.**
  - **Phase 4 closed on engineering 2026-09-13; the launch line is the only thing outstanding.** Track A: L0/L1/L2 collecting, L3 run and returned a well-powered null (pre-registered at `e075a5e`, recorded at `d63e135`), L4 recorded as not applicable at this sample. Track B: top-of-book data layer, `/arbitrage`, `/price-pair/$sym`, the every-direction pairs table, and a fee model defined in `packages/core` and deliberately not displayed until member settings can supply per-venue fees. Plus `/status` and the geo-probe footer link.
  - **Two gates remained, and neither was code. One is now met.** Live venues stand at **15** against the 15–20 target as of 2026-09-13 (Binance is what closed it), so the count gate is cleared — but *thinly*: 6 of the 21 built venues return zero markets (`hl-cash`, `hl-flx`, `hl-hyna`, `hl-vntl`, `hl-km`, `hl-abcd`), so a single breakage drops back under 15. Treat 15 as a floor to build headroom above, not a finish line. `robots.txt` still returns `Disallow: /` — `app.ts` says why: "keep the site out of search engines until the legal checklist is done", i.e. `plans/phase0-referrals-legal.md`, which stands at **25 unchecked items and 0 checked**, plus 9 ordered next actions. The legal checklist runs in parallel with adapter work; it gates publication, not building.
  - **The venue gate and Phase 5 are the same task.** Closing it means building adapters, which is Phase 5's entire content — so Phase 5 starts now rather than after launch, and the first family to land also clears the MVP count.

### Phase 5 — Scale to ~57 venues (weeks 8–18, run in parallel across devs)

> **Opened 2026-09-13. The backlog is exactly 41, and it is already enumerated in the catalog.**
>
> - **Read `plans/symbol-identity-refactor.md` first: steps 1–3 of it gate the venue work.** Adding venues multiplies an identity bug that is already live. `base` is serving as both the venue's ticker and the cross-venue asset key, and all three failure modes are measured: collision (`CAT` 387,440,758×, a memecoin and Caterpillar; `STX` 2,963×; `BB` 955×), fragmentation (the S&P 500 under `SPX500`/`US500`/`SP500` across six venues), and scale variance (`hl-mkts:US500` at 760.96 against ~7,620 with `multiplier: 1`). WEEX and Bullet add 438 and 11 TradFi perps respectively, so each new venue makes the conflation worse rather than revealing it. Steps 1–3 also change *which markets pair*, so landing them after a venue launch would move the rankings twice.
>
> - **21 built, 61 catalogued, 39 unbuilt** (recounted from the catalog itself on 2026-09-13, superseding the "20 built / remaining 41" first written here). `FIXED_ADAPTERS` holds **eleven** — bybit, okx, gate, mexc, kucoin, aster, hyperliquid, dydx, paradex, lighter, **binance** — and `createAdapters` *synthesises* an adapter for any `type: "hip3"` venue carrying a `hip3Dex`, so the ten Hyperliquid sub-dexes are covered by construction rather than written by hand. 61 catalogued less 11 fixed, less 10 synthesised, less 1 alias leaves **39**: 11 CEX and 28 DEX.
> - **Do not read "not built" off market counts.** Six HL sub-dexes return zero markets while running cleanly, and `hl-flx` and `hl-abcd` additionally fail 5–6 runs a day. Those are built-and-broken, not unbuilt, and `/status` now distinguishes them: `planned` means the collector has never run it, `silent` means it ran within retention and stopped.
> - **The first family is the cheapest win and it is partly done.** `binance-fapi` covers Binance, Aster, WEEX and Bullet — and `asterAdapter` already exists, so the base can be extracted from working code rather than written blind. Binance is the largest venue missing from the site, so this family alone likely clears the 15–20 MVP venue gate.
> - **Sequencing rationale.** Families before singletons: `binance-fapi` (4 venues), then `orderly` (WOOFi Pro plus other brokers), then the bulk "all markets" CEX endpoints already listed below, which are one cheap call each. The one-call-per-symbol and WebSocket venues are last, since they cost the most per venue collected.
> - **Expect breakage, and measure it.** The plan budgets ~20% of ongoing time for adapter breakages at 57 venues. `/status` is now the instrument for that: failure counts over 24h per venue, rather than a pass/fail that hides a venue answering cleanly with nothing behind it.
- For each venue: adapter + fixtures + contract test + `venues.json` entry (logo, referral, fees, hint). Simple CEX adapters ~0.5 day; unusual DEXs 1–3 days. Taker fees aren't published consistently, so `venues.json` holds them, verified by hand.
- **Adapter families (build the base once, then configure each venue):**
  - `binance-fapi` base: Binance, Aster, WEEX, Bullet (Binance-compatible APIs).
  - `hyperliquid` base: HL core + every HIP-3 dex via `perpDexs`/`metaAndAssetCtxs{dex}`. Dex ids from `perpDexs` (2026-09-11): `xyz`, `flx` (Felix), `hyna` (HyENA), `km`/`mkts` (Kinetiq), `vntl` (Ventuals), `cash` (dreamcash), `para` (Paragon), `io` (Entropy), `abcd` (unidentified). Coins are prefixed, e.g. `xyz:XYZ100`. Bullpen is a Hyperliquid front-end, an alias with no markets of its own.
  - `orderly` base: WOOFi Pro + other Orderly brokers, deduped.
- **Bulk "all markets" endpoints (cheap):**
  - CEX: Binance `premiumIndex`, Bybit `tickers`, Bitget `current-fund-rate`, Gate `contracts`, KuCoin `contracts/active`, HTX `swap_batch_funding_rate`, BingX `premiumIndex`, BitMart `details`, Pionex `indexes`.
  - DEX: Paradex `markets/summary?market=ALL`, dYdX `perpetualMarkets`, Extended `info/markets`, Variational `metadata/stats`, Reya `perpMarkets/summary`, edgeX `getLatestFundingRate`.
- **One call per symbol (spread across alarm cycles):** OKX (verify whether `instId=ANY` works), MEXC, BloFin, Hotcoin, CoinW, Aevo, Ethereal (≤50 ids per call).
- **Tight rate limits, so use WebSocket from the DO:** Lighter (60 req/min without auth), Phoenix (WS-first), Perpl (10 req/min, 16 subscriptions).
- **No funding-history API, so the history DOs are the only source (they record from day 1):** Variational, Reya, LBank. Short lookback: Ethereal ~1 month, Velocity ~30 days.
- **Remaining CEX:** KuCoin, HTX, BingX, BloFin, BitMart, Toobit (symbols like `BTC-SWAP-USDT`), Hotcoin, WEEX, CoinW, LBank, Pionex.
- **Remaining DEX:** WOOFi Pro/Orderly, ApeX Omni, edgeX, Variational (RFQ quotes at $1k/$100k/$1m instead of a book), GRVT, Ethereal, Extended (needs a User-Agent), Aster, **Velocity (formerly Drift, relaunching)**, **Nado (formerly Vertex, now on Ink)**, Reya, Backpack, Aevo, Bluefin, Phoenix, Bullet, Perpl, 01/N1.
- Budget about 20% of ongoing time for adapter breakages (expect 1–3 per week at 57 venues). A nightly live smoke test flags schema drift.

### Phase 6 — Post-MVP
- WebSocket streams for tier-1 price arb (DO outbound WS, reconnect every 15 min on alarm) plus a hibernating LiveFeed DO.
- Airdrop calendar, points calculator, blog/glossary (MDX), en/ru with hreflang, Telegram spread alerts, public read API.

**Rough monthly cost:** ~$30–120. Workers Paid $5; DO alarms ~$5–15; Pipelines/R2/R2 SQL <$20; D1 ~$5; Python Container on demand <$10; relay (if needed) ~$10–40.

> **Workers Free (current account):** Containers and Pipelines are unavailable. Free's limits (10ms CPU and 50 subrequests per invocation, 100k requests/day) don't fit Phase 1's 60s polling of ~57 venues with 1–2 MB bulk responses. Phase 0 recommends upgrading to Paid before Phase 1; see [`phase0-report.md`](phase0-report.md) for the Free-plan substitutes (R2 NDJSON archive, backfills from CI, fallback rungs 3/4 only).

---

## Sequencing from here (written 2026-09-13, after the identity gate shipped)

Everything below is ordered by what unblocks the most, not by what is easiest.

**1. Make the ranking reward persistence rather than width — cheapest change here, and it decides
whether anything else is worth scaling.** Measured 2026-09-13 and written up in
[`product-market-fit.md`](product-market-fit.md): consecutive nightly runs share six of their seven
days, yet only **8 of 23** viable pairs survive from one run to the next, the top-20 keeps 10, and
the chosen venue pair changes for **371 of 524 assets (71%)**. A likely cause sits in our own SQL —
`screener_pairs` picks each asset's representative pair with `DISTINCT ON (base) ... ORDER BY spread
DESC`, i.e. the *widest* spread, which selects outliers by construction — and every re-selection is
a re-entry that pays the ~$20 of fees already erasing the median row.
  - **Measure before changing.** Isolate whether the churn is leg re-selection or genuine regime
    change: hold the legs fixed across the two runs and re-score.
  - **Then try** selecting legs by settled `spread_apr_7d`, or by spread weighted with
    `pair_stability` — both already returned by the function, so this costs no new scan.
  - **Pre-registered success criterion:** consecutive-run overlap of the viable set rises from 8/23
    to **≥15**, and same-legs from 29% to **≥60%**, without the viable set's median net funding
    falling more than a quarter. Re-measure on two *non-overlapping* windows (from ~2026-09-26);
    today's figure is a churn floor, not a persistence measurement.
  - **Ship net-of-fees alongside it.** `packages/core/src/fees.ts` exists and nothing renders it, so
    every figure on the site is currently gross — and the median row is negative once four fills are
    charged. This is an integrity fix as much as a product one.

**2. Asset class — identity refactor step 2, migration 017.** The gate shipped in 016 is
*lossy*, and that is the argument for doing this next. Because `base` still cannot hold two assets
under one ticker, the gate keeps one cluster and discards the other: `BB` keeps BlackBerry's three
venues and throws away BounceBit's five ($1.83M of open interest), and `PURR`, `ON`, `AI`, `STX`
and `CAT` all lose a side the same way. `(asset_class, base)` makes `equity:STX` and `crypto:STX`
different keys, so **both sides become listable instead of one being discarded**. It also has to
land before WEEX (+438 TradFi perps) and Bullet (+11), each of which multiplies exactly this
collision.

**3. Start the legal and affiliate track now, in parallel — it is the real critical path.** It is
also the only track that is not code, and its lead times are external: counsel review, entity
setup, KYC'd accounts, affiliate applications, and written data-consent from Binance, OKX, MEXC,
KuCoin, Aster and Paradex. Engineering owes just two of the 25 items — geo-gating the referral CTAs
in the Worker, and shipping the disclosure/disclaimer text. Adapter work cannot shorten this, so
starting it late is what would delay launch.

**4. WEEX and Bullet, then the rest of the `binance-fapi` family.** The largest venues still
missing, the base is extractable from the working `asterAdapter` rather than written blind, and it
buys headroom above the 15-venue floor. Both need the pluggable tradability, interval source and
timestamp scale noted in refactor step 5.

**5. Work the verification report as a list.** `scale` verdicts are a to-do, not just a finding:
`hl-mkts:US500` sits at exponent −1 on a correlation of 0.813, and setting that multiplier returns
a real market to the scan. Re-read `/status` after each large venue lands.

**6. Then the remaining families in cost order** — `orderly`, the bulk "all markets" CEX endpoints,
the one-call-per-symbol venues, and the WebSocket-only venues last. Refactor step 6
(cross-stablecoin, 23.3% of live pairs) lands after steps 1–3, not before, or it filters a pairing
that is about to change.

---

## Verification
- **Unit (`bun test`):**
  - Annualization per unit/basis/interval.
  - Sign and predicted-vs-settled handling.
  - Spread selection with filters and outlier guards.
  - Slippage book-walk, symbol/multiplier mapping.
  - Backtest math on hand-computed fixtures: 1h leg vs 8h leg, an interval change mid-window, a missing settlement, 4 fills.
- **Stability and momentum:** computed by the collector in SQL (migration 009) and covered by integration tests in `apps/collector/src/store.int.test.ts` against the `airates_it` schema. *Correction 2026-09-12:* this line previously read "**Python (`uv run pytest` in py/analytics):** stability and momentum against synthetic series. Parity test: the nightly Python backtest matches the TS engine within 1e-6 on shared fixtures." There is no `py/analytics` package and no nightly Python backtest; `packages/core/src/backtest.ts` is the only engine, so there is nothing for it to be at parity with.
- **Adapter contract tests:** `packages/adapters/__fixtures__/<venue>/*.json` → valid zod objects. The nightly live smoke test hits real endpoints.
- **Cross-check:** BTC/ETH/SOL APR per venue vs each venue's own UI and vs Hyperliquid `predictedFundings` (Binance/Bybit/HL), tolerance < 0.5 APR pts. Settled sums vs the venue's own funding-history UI.
  - **Done 2026-09-13 against `predictedFundings`, and it passes.** Worst divergence **0.0859 APR points** against the 0.5 tolerance, across BTC/ETH/SOL on the two venues Hyperliquid also publishes. Three of the six comparisons are **exactly** zero to ten decimal places (`ETH bybit`, `SOL bybit`, `SOL hyperliquid`); the non-zero three are 1e-7 to 3.6e-7 on the raw rate, which is two publishers sampling a continuously-moving *predicted* rate seconds apart, not an arithmetic fault.
  - **What makes it a real check rather than a tautology:** our own `sum(rate)/basis_hours × 876000` was applied to *their* rate and reproduced our stored APR. That is what would expose a systematic annualization error — the one class of bug every internal test would happily agree with, since they all share the formula. It also confirms Hyperliquid's 1-hourly interval is handled correctly against Bybit's 8-hourly.
  - **Gap worth naming:** `predictedFundings` also reports `BinPerp`, and **we do not collect Binance at all**, so that column cross-checks nothing. The largest perp venue is absent from our 14. The venue-UI half of this item still needs a human with a browser.
- **Local E2E:**
  1. `wrangler dev` for `ingest` + `web` (Miniflare DOs/D1).
  2. Trigger the watchdog via `curl "localhost:8787/cdn-cgi/handler/scheduled"`.
  3. Open `/screener` and `/pair/BTC?long=hyperliquid&short=binance&size=10000&days=30` in Chrome (claude-in-chrome); confirm rows, freshness stamps and the equity curve.
- **Staging soak (24h):** all venues fresh in `/v1/health`, alarm overruns = 0, DO CPU p99 within limits, Pipelines lag < 2 min, R2 SQL query over the first day returns the expected row counts.
